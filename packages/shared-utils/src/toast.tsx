// Placeholder -- real implementation moved to sibling `toast.ts` (API)
// and `ToastHost.tsx` (React component) to satisfy the
// react-refresh/only-export-components rule.
//
// This `.tsx` file cannot be deleted from the current sandboxed session.
// Resolver note: both TypeScript and Vite prefer `.ts` over `.tsx` when no
// extension is given, so `@/lib/toast` resolves to `toast.ts`; this file
// is orphaned and safe to `git rm` after commit lands locally.
export {};
