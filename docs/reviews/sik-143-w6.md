---
type: review
status: pass
owner: lhr
last-reviewed: 2026-05-30
issue: SIK-143
wave: W6
contract: docs/plan/sik-143-home-bottomrow-density-visual-contract.md
mode: Reviewer
---

> **Runner 收口更新 (2026-05-30)**：F1（P2，唯一阻塞项）已修复——新增
> `apps/web/src/views/Home/sections/RecentPracticeSection.test.tsx`（3 用例：
> ready 渲染 + `data-scrollable` 开/关两态断言），最近练习 cap=1 的滚动契约现有
> vitest 守护，名实相符。F2（提交拆分）按 H9 落实：W5 token / W6 实现+测试 / docs
> 三组独立 commit。F3/F4 为 P3 观察项，不阻塞。复核后本波转 **pass**。Home 全套件
> 275 passed（31 files）。

# SIK-143 W6 独立审查 — 底栏 feed 卡滚动密度 + 遗留 token 收尾

> Reviewer Mode（只读，不改源码）。依据：lhr 2026-05-30 口头追加需求 + 契约
> `docs/plan/sik-143-home-bottomrow-density-visual-contract.md` §3.1（W6 列表滚动密度）。
> Runner 浏览器实测数据（1440/1920）由本任务给出，本审查据其交叉核验，未重跑 browser。

## 1. 检查范围

git diff 范围：`git --no-pager diff -- apps/web/src/views/Home/ docs/`（仓库根 d:\py_pj\sikao）。

本次 W5/W6 触及的运行时 + 测试文件（6 个，+83 / -6）：

- `apps/web/src/views/Home/sections/MetricRow.module.css`（裸 9999px → token）
- `apps/web/src/views/Home/sections/RecommendationSection.module.css`（滚动密度 + fade + density）
- `apps/web/src/views/Home/sections/RecommendationSection.tsx`（cap 常量 + data-scrollable）
- `apps/web/src/views/Home/sections/RecommendationSection.test.tsx`（W6 滚动断言 ×2）
- `apps/web/src/views/Home/sections/RecentPracticeSection.module.css`（滚动 + fade + flex）
- `apps/web/src/views/Home/sections/RecentPracticeSection.tsx`（cap 常量 + data-scrollable）

docs 侧改动（非本次审查重点，仅 ledger/frontmatter 对齐）：契约 §3.1 追加、Web-Layout §5
owner 表追加、SIK-138~142 plan/contract 归档与 review frontmatter `active→pass`、
Migration-Status / Home README ledger note。已抽查，无契约语义冲突（见 F2）。

交叉核验文件：`Home.tsx`、`RecommendationCard.tsx`（确认 list 子项挂 `.feedItem`）。

## 2. 逐维度核对（带证据行号）

### 维度 1 — 拥挤根因是否修复 ✅

- 今日推荐 `.feedItem` 由隐式可压缩（flex column + gap 下默认 `flex-shrink:1`）改为
  `flex: 0 0 auto`（RecommendationSection.module.css:98），子项保持自然高度不被压扁。
- 最近练习 `.feedItem` 同样补 `flex: 0 0 auto`（RecentPracticeSection.module.css:75）。
- density 收紧三处均带 spacing-allow 理由：`padding: 6px var(--space-3)`（:107）、
  `feedMain gap: 2px`（:146）、`feedSub line-height: 1.25`（:161），与契约 §3.1 一致。
- `RecommendationCard.tsx:42` 根 `<li className={styles.feedItem}>`，确认 flex 规则作用于
  真实 list 子项，而非空挂。结论：根因（被压缩到 ~39px）已修复为自然高度 + 收紧密度。

### 维度 2 — 可视条数 ✅（带一处观察 F4）

- 今日推荐 `max-height: calc(3 * 50px + 2 * var(--space-2) + 0.2 * 50px)`
  （RecommendationSection.module.css:74），与契约 §3.1 表格逐字一致（3 满 + 0.2 peek）。
  Runner 实测 1440/1920 = 3.15/3.15，落在 ~3.2 目标区间。✅
- 最近练习无 `max-height`，由 rightStack 格高（1fr）+ `min-height:0` + `overflow-y:auto`
  约束，符合契约「由 rightStack 格高约束」。Runner 实测 1.34/1.53，落在 ~1.5 目标。✅
- Runner 实测两卡均 scrollable / canScroll / scrollbarGutter=0 / screen locked / no page scroll。

### 维度 3 — 隐藏滚动条保留滚动 ✅

今日推荐 list：`overflow-y: auto`（:76）+ `scrollbar-width: none`（:77）+
`-ms-overflow-style: none`（:79）+ `.list::-webkit-scrollbar { display: none }`（:81-83）。
最近练习 list：`overflow-y: auto`（:51）+ `scrollbar-width: none`（:53）+
`-ms-overflow-style: none`（:55）+ `::-webkit-scrollbar`（:57-59）。
三件套齐全，overflow-y:auto 保留，滚动功能不丢。✅
（编译器告警 `scrollbar-width` 在旧 Chrome/Safari 不支持仅为渐进增强提示，webkit 伪元素已兜底，非缺陷。）

### 维度 4 — 渐隐遮罩 ⚠️（含 P2 守护缺口 F1）

- mask 仅挂在 `.list[data-scrollable]`（RecommendationSection.module.css:90-94 /
  RecentPracticeSection.module.css:62-66），短列表不裁末行，符合契约。✅
- `data-scrollable` 由组件按 `items.length > cap` 计算：
  今日推荐 cap=3（RecommendationSection.tsx:33, 101），最近练习 cap=1
  （RecentPracticeSection.tsx:34, 53），cap 取值与契约 §3.1 末行一致。✅
- vitest 守护：今日推荐有断言 ×2（RecommendationSection.test.tsx:176-189 scrollable=true /
  190-201 not scrollable），覆盖开/关两态。✅
- **缺口（F1，P2）**：契约 §3.1 末行写明「今日推荐 cap=3 / **最近练习 cap=1**，…并有
  vitest 断言守护」。最近练习侧无任何 vitest 断言——全仓不存在 `RecentPracticeSection.test.tsx`，
  `Home.test.tsx` 也无 `home-recent` / `data-scrollable` / `RecentPractice` 覆盖（grep 全空）。
  即最近练习的 `data-scrollable` 计算只在浏览器实测被间接验证，无回归守护，与契约文字承诺不符。

### 维度 5 — a11y ✅

- mask-image / scrollbar 隐藏均为纯视觉，DOM 结构与 tab/SR 可达性不变；列表 `overflow-y:auto`
  仍可键盘滚动到末行。✅
- 4 卡 aria-label 完好：Home.tsx:61/65/70/73 分别 `今日推荐 / 学习进度 / 本周备考回顾 / 最近练习`，
  且各保留 `data-testid="panel"`。刷新 icon-btn `aria-label="刷新推荐"`（RecommendationSection.tsx:50）
  未破坏。本次 W6 改动未触碰 a11y 相关属性。✅

### 维度 6 — Token / 反模式 ✅

- mask 用 `black` / `transparent` 关键字（alpha 遮罩语义正确，非 hex 调色），符合契约要求。✅
- 非 token 值均带 spacing-allow 理由：`max-height` calc（:72-74 注释）、padding 6px（:107）、
  gap 2px（:146）、line-height 1.25（:161）。✅
- 反模式扫描：Home sections CSS 无 `color-mix` / `--paper-*` / `--ink-*` / `--r-card` /
  `--r-pill` / 裸 hex（grep 全空，唯一命中是 MonthEventChip 注释里的「no hex」说明文字）。✅
- 遗留收尾：`MetricRow.module.css` 裸 `9999px` → `var(--radius-999)`（:104），
  全仓 `apps/web/src/**/*.css` 已无 `9999px`（grep 0 命中）。✅ 满足追加需求 1。

### 维度 7 — 一屏锁死未破 ✅

- 两 list 均 `min-height:0 + overflow-y:auto` 局部滚，未引入整页滚。
- 今日推荐 `max-height` 为固定上限（≈174px），小于第 4 行锁定格高，不会顶破 row。
- Runner 实测 locked=true / no page scroll，符合 Web-Layout 一屏锁死。✅

### 维度 8 — 回归 ✅

- diff --stat 确认仅 6 文件改动；ProgressSection / WeeklyReviewSection 的 `.module.css`/`.tsx`
  **未出现在 diff**，列定义（Home.module.css `.bottomRow`/`.rightStack`）未改，
  共享组件（Panel/ScreenLockShell/RecommendationCard 逻辑）未改。
- WeekCalendar / MonthCalendar 的 data-scrollable 测试与本次无关（既有）。无误伤。✅

## 3. 发现项

| # | 严重度 | 维度 | 证据 | 描述 |
|---|---|---|---|---|
| F1 | P2 | 4 | RecentPracticeSection.tsx:34,53；全仓无 RecentPracticeSection.test.tsx；Home.test.tsx 无 recent/scrollable 覆盖 | 最近练习 `data-scrollable`（cap=1）无 vitest 断言守护，与契约 §3.1「并有 vitest 断言守护」承诺不符。今日推荐侧已守护（test.tsx:176-201），但最近练习仅靠浏览器实测，无回归网。 |
| F2 | P3 | 范围外 | docs/plan/sik-138~142 frontmatter；docs/reviews/sik-140/141 `active→pass` | 本次 diff 夹带大量 SIK-138~142 ledger/归档改动，与 W6（SIK-143）非同一关注点。语义无冲突，但混在同一 working tree；提交时应与 W6 实现拆为独立 commit（H9 原子提交），勿混合。 |
| F3 | P3 | 2 | RecommendationSection.module.css:74 | `max-height` 用魔术值 `50px`（item 实测高）硬编码 ×2 + `0.2` 系数。已带 spacing-allow 注释、契约逐字一致，可接受；但若后续 feed-item 字号/padding 再调，50px 需手工同步，存在漂移风险。建议注释中标注「与 .feedItem 实测高耦合」。 |
| F4 | P3 | 2 | Runner 实测 最近练习 1440=1.34 | 1440 下最近练习 1.34 行略低于契约「~1.5」叙述（1920=1.53 达标）。因右栈格高随视口变化属预期自然约束，非缺陷；记录为观察项，无需改动。 |

> 无 P0 / P1。F1 是唯一需处理项（守护缺口），其余为提交纪律 / 可维护性观察。

## 4. 建议处理

- **F1（P2，建议本波补齐）**：新增 `RecentPracticeSection.test.tsx`，对齐
  `RecommendationSection.test.tsx:176-201` 模式：mock `useProfileRecords` 返回
  2 条 → 断言 `ul` 有 `data-scrollable="true"`；返回 ≤1 条 → 断言无该属性。
  使契约 §3.1「最近练习 cap=1 … vitest 断言守护」名实相符。
- **F2（P3）**：提交时按 H9 拆分——W6 实现/测试一组 commit，docs ledger/归档一组 commit，
  勿混合 plan/实现/测试。
- **F3（P3）**：可选。在 `max-height` calc 注释补一句「50px = .feedItem 实测高，调密度时同步」。
- **F4（P3）**：仅记录，无需改动。

## 5. 风险等级

**低（Low）**。改动局限于两个 feed section 的 CSS 局部滚 + 一个 token 收尾，无运行时逻辑/
契约/鉴权/DB 影响；一屏锁死与 a11y 未破；今日推荐侧有双向断言守护。唯一实质项 F1 是测试覆盖
缺口（非功能缺陷），浏览器实测已间接验证行为正确，回归暴露面小。

## 6. 总体结论

**changes-required**

追加需求 1（MetricRow 9999px→token）与需求 2 的视觉/滚动/渐隐/密度/a11y/一屏维度均已正确落地，
今日推荐侧守护完整。但契约 §3.1 明确承诺「最近练习 cap=1 … 有 vitest 断言守护」，实际最近练习
侧无任何断言（F1，P2），名实不符。建议补齐 `RecentPracticeSection.test.tsx` 后即可转 pass；
F2 提交拆分为交付纪律要求。若 lhr 判定浏览器实测已足够、可将 F1 降级为 follow-up，则本波可按
conditional-pass 收口，但契约文字需同步弱化「vitest 断言守护」表述以免 SSOT 漂移。

---

Reviewer · 2026-05-30
