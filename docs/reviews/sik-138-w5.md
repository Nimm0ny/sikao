# Scope

- Mode: `Reviewer Mode`
- Review target: `SIK-138` W5 retrospective review
- Commit scope:
  - `71dca7cc5` `feat(home-calendar): introduce MonthEventChip + chip tokens (SIK-138 W5)`
  - `bff3889cf` `test(home-calendar): cover MonthEventChip channels (SIK-138 W5)`
- Files reviewed:
  - historical `MonthEventChip.tsx`
  - historical `MonthEventChip.module.css`
  - historical W5 tests
  - token additions tied to the original chip channel system
- Focus:
  - multi-channel chip contract (`kind/title/category/status/source/linkedSession/target`)
  - optional field handling
  - accessibility of channel subsets
  - token consumption

# Findings

1. `Medium` Historical optional-ID handling was too narrow: the W5 chip logic checked `!== null` and could misrender `linkedSession` / `target` when the API shape surfaced `undefined`.
   Evidence anchors:
   - `packages/api-client/src/types/api.generated.ts@71dca7cc5:4410,4433`
   - `apps/web/src/views/Home/sections/MonthEventChip.tsx@71dca7cc5:146,154`
   - `apps/web/src/views/Home/sections/MonthEventChip.test.tsx@bff3889cf:75`
2. `Medium` Historical channel-subset accessibility was incomplete: in some legal `visibleProperties` subsets, rendered icons could become visually present but hidden from the accessibility tree.
   Evidence anchors:
   - `apps/web/src/views/Home/sections/MonthEventChip.tsx@71dca7cc5:142-145`
   - `apps/web/src/views/Home/sections/calendarViewConfig/factory.ts@71dca7cc5:164-167`
   - `apps/web/src/views/Home/sections/calendarViewConfig/factory.test.ts@bff3889cf:152-159`
3. `Medium` Historical channel leakage existed: `status` could still leak `kind` semantics when `kind` itself was turned off.
   Evidence anchors:
   - `apps/web/src/views/Home/sections/MonthEventChip.tsx@71dca7cc5:70-79,126-127`
   - `apps/web/src/views/Home/sections/MonthEventChip.module.css@71dca7cc5:31-34,89-92`
   - `apps/web/src/views/Home/sections/calendarViewConfig/factory.test.ts@bff3889cf:152-159`
4. `Low` Historical token drift: `--cal-chip-gap` was introduced but not actually consumed by the CSS.
   Evidence anchors:
   - `packages/design-system/src/tokens.css@71dca7cc5:761`
   - `apps/web/src/views/Home/sections/MonthEventChip.module.css@71dca7cc5:13,68`

Resolution status on current `main`:
- Superseded by later Home calendar redesign work.
- The active chip no longer carries the original W5 seven-channel surface:
  - current `MonthEventChip.tsx:81-130` only renders the reduced surface (`kind` icon, title, aggregate line, done check, tone metadata)
  - `source / linkedSession / target / category / status-dot` moved off-chip into the peek path under later waves (`SIK-142` W1/W5 and `SIK-141`)
- Therefore the historical W5 issues are no longer live blockers on current `main`.

# Visual Contract Cross-Check

| Check | Result | Evidence |
| --- | --- | --- |
| Current chip no longer exposes the original W5 multi-channel surface | PASS | active `MonthEventChip.tsx` is intentionally simplified by later visual-contract decisions |
| Off-chip channels now live in peek/detail instead of the chip body | PASS | current Home peek tests assert `source/linked/target/notes` in peek, not chip |
| The historical W5 token family is no longer the active UX contract | PASS | current chip contract is owned by later calendar visual-unify work |

# Suggested Actions

- Keep the W5 historical findings on record; they explain why the later chip redesign was justified.
- Do not reopen the original W5 surface unless the later visual-contract decisions are explicitly reverted.
- No current-main runtime fix is required for these W5-specific findings because the surface they targeted has been replaced.

# Risk Level

- Historical snapshot risk: `medium`.
- Current main residual risk: `low`.
- Reason:
  - The W5 findings were valid for the original seven-channel chip.
  - That surface is no longer active on `main`, so the findings are now historical, not unhandled blockers.

# Decision

`review pass`

Note:
- This is a retrospective pass for debt closure after later redesign, not a claim that the original W5 snapshot itself had zero issues.
