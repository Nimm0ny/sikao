# Scope

- Mode: `Reviewer Mode`
- Review target: SIK-138 W7 closeout evidence on branch `feat/sik-138-w7` (head `60d9022be`)
- Issue: `SIK-138` — https://www.notion.so/36ebc174f6c88187840ac2623a1666f7 (Status `In Progress`)
- Truth source: `docs/plan/sik-138-home-calendar-notion-like-visual-contract.md` §7 (Acceptance Hooks) + §8 (W7 Closeout Evidence)
- Basis files:
  - `docs/plan/sik-138-home-calendar-notion-like-visual-contract.md`
  - `docs/reviews/sik-138-w1.md`
  - `docs/reviews/README.md`
  - `.tmp_review/out/sik-138-w1/` (16 screenshots + `axe-home-calendar.log`)
  - `apps/web/src/views/Home/sections/CalendarPanel.tsx`
  - `apps/web/src/views/Home/sections/calendarViewConfig/factory.ts`
  - `apps/web/src/views/Home/sections/MonthCalendarView.tsx`
  - `apps/web/src/views/Home/sections/WeekCalendarView.tsx`
  - `apps/web/src/views/Home/sections/peek/CalendarPeekProvider.tsx`
  - `apps/web/src/views/Home/sections/peek/CalendarPeekHead.tsx`
  - `apps/web/src/views/Home/sections/peek/CalendarPeekProperties.tsx`
- Review checklist (per task): §7 table completeness, 16-screenshot integrity/naming/path, §8 closeout accuracy, a11y evidence, commit hygiene.

## Preflight

- `git`: available. `git branch --show-current` → `feat/sik-138-w7`; `git log --oneline -8` confirms the 5 W7 commits present and ordered; `git rev-parse main` == `git rev-parse origin/main` == `4dfc057f5757f8baf2f692ce3a42e4f321e58814` (no branch-anomaly residue). Working tree clean.
- Chrome DevTools MCP: available (`list_pages` → `about:blank`). Not needed for re-render; cross-check done against archived PNGs + §8 descriptions + source.
- Notion MCP: available (`notion_search` resolved issue page).
- shell: available (PowerShell; `&` separator not allowed, used `;`).
- Independent subagent review: this report IS the independent review.

# Findings

Summary count: `0 high / 0 medium / 2 low`

- **F1 (low)** — Command-string inconsistency for the a11y evidence. §7 旁注 (`docs/plan/...visual-contract.md` Acceptance Hooks, a11y row) lists the command as `pnpm --filter @sikao/web test -- --run src/views/__tests__/views.a11y.test.tsx`; §8.1 Evidence Index lists `npm --prefix apps/web run test:a11y`; the archived log (`.tmp_review/out/sik-138-w1/axe-home-calendar.log`) shows `vitest run src/views/__tests__/views.a11y.test.tsx` via the `test:a11y` script. All three resolve to the same vitest a11y suite and the same target file, and the log shows `13 passed (13)` exit 0. The discrepancy is cosmetic (package-manager wrapper + script alias), not an evidence gap. Suggest aligning the §8.1 command text with §7, or adding a one-line note that `npm run test:a11y` is the script alias of the §7 command.
- **F2 (low)** — §7 row `overflow 文案为 +N 更多` cites both `MonthCalendarView.tsx` and `WeekCalendarView.tsx` as 实现位置, but only `MonthCalendarView.tsx:165-166` actually renders `+{overflow} 更多`; `WeekCalendarView.tsx` has no overflow/更多/`cardLimitPerCell` path (grep returned no matches). This is already disclosed and reasoned in §8.3 drift row 3 (week lanes never overflow with current cell sizing; row stays PASS via Month implementation), so it is a declared nuance rather than an undeclared drift. Suggest the §7 实现位置 cell drop the `WeekCalendarView.tsx` reference (or annotate "Month only") to avoid implying a Week overflow path that does not exist.

Neither finding blocks `Status=Done`.

# Visual Contract Cross-Check

§7 has 16 rows; all marked `PASS`. Each verified below against a concrete code/evidence pointer.

| # | §7 Hook | Result | Evidence |
| - | ------- | ------ | -------- |
| 1 | contract 已落档且 issue Acceptance 引用 | PASS | contract committed at `60414c801`; issue `SIK-138` `## Acceptance` references the contract path (Notion search highlight confirms W1 contract landed) |
| 2 | segmented tabs 三态存在且可切换 | PASS | `CalendarPanel.tsx:48-51` `SEGMENT_ITEMS = 今日/本周/月` exactly 3; impl 1440 snapshot (§8.1) shows tab switch |
| 3 | `prev / today / next / +` 四按钮齐全 | PASS | `CalendarPanel.tsx:123-146` view-aware prev / `回到今天` / next / disabled `新建事件` |
| 4 | countdown chip 存在并与四按钮同列 | PASS | `CalendarPanel.tsx:147-153` `国考 D-138` chip in same `panelActions` row |
| 5 | month 默认 `cardLimitPerCell=3` | PASS | `factory.ts:59-63` `DEFAULT_LIMIT_PER_VIEW.month = 3`; `MonthCalendarView.tsx:116,138` slices by `cardLimitPerCell` |
| 6 | overflow 文案为 `+N 更多` | PASS (see F2) | `MonthCalendarView.tsx:165-166` renders `+{overflow} 更多`; Week has no overflow path (declared §8.3 row 3) |
| 7 | chip click 打开 Peek | PASS | `MonthCalendarView.tsx` Peek provider mount + §8.1 impl click → `dialog ... modal` |
| 8 | Peek 顶 bar 6 按钮齐全 | PASS | `CalendarPeekHead.tsx:48-110` exactly 6: 展开为页面/上一条/下一条/复制链接/更多操作/关闭 |
| 9 | Peek 8 行属性齐全 | PASS | `CalendarPeekProperties.tsx:66-107` exactly 8: 时间/类型/分类/状态/来源/关联会话/目标/重复 |
| 10 | Peek close 后 restore focus | PASS | `CalendarPeekProvider.tsx:59,67-76` captures `document.activeElement`, restores via RAF; §8.1/§8.2 confirm 1440+1920 `body.overflow=visible` + focus back on chip |
| 11 | `1440` Chrome MCP 双开验收 | PASS | `prototype-1440-home-calendar.png` (449143 B) + `implementation-1440-home-calendar.png` (135833 B) archived, valid PNG |
| 12 | `1920` Chrome MCP 双开验收 | PASS | `prototype-1920-home-calendar.png` (449980 B) + `implementation-1920-home-calendar.png` (126408 B) archived, valid PNG |
| 13 | Peek `open` 截图 | PASS | `{prototype,implementation}-{1440,1920}-peek-open.png` all present, >0 B, valid PNG |
| 14 | Peek `next-prev` 截图 | PASS | `{prototype,implementation}-{1440,1920}-peek-next-prev.png` all present, >0 B, valid PNG; §8.1 documents same-chain title transitions |
| 15 | Peek `close` 截图 | PASS | `{prototype,implementation}-{1440,1920}-peek-close.png` all present, >0 B, valid PNG |
| 16 | a11y 命令已执行并归档 log | PASS (see F1) | `axe-home-calendar.log` shows `Test Files 1 passed (1)`, `Tests 13 passed (13)`, exit 0 |

## Screenshot Integrity (16 files)

- `list_directory` + `Get-ChildItem` confirm exactly the 16 required filenames under `.tmp_review/out/sik-138-w1/` (8 prototype + 8 implementation), plus `axe-home-calendar.log` and the W1 lint/test/typecheck logs.
- All 16 PNGs are >0 bytes (smallest `prototype-1440-peek-close.png` 111855 B; largest `prototype-1920-home-calendar.png` 449980 B) and start with a valid PNG signature `89 50 4E 47`.
- Naming matches §7 spec exactly: `{prototype,implementation}-{1440,1920}-{home-calendar,peek-open,peek-next-prev,peek-close}.png`.
- Archive path `.tmp_review/out/sik-138-w1/` matches §7 explicit override (not the default `.tmp_review/visual-diff/SIK-138/`).

## .gitignore Override Confirmation

- `.tmp_review/.gitignore` contains a single rule `*`.
- `git check-ignore -v --no-index` resolves `.tmp_review/out/sik-138-w1/axe-home-calendar.log` to `.tmp_review/.gitignore:1:*`.
- Therefore all evidence under this directory was `git add -f` forced into history. This is the issue Acceptance + contract §7 explicit archive override, **confirmed as known and necessary — not a finding**.

## §8 Closeout Accuracy

- **§8.1 Evidence Index** — 16 rows, each maps to a verifiable pointer (source line, snapshot description, or archived file). All cross-checked above. Header note "Generated against ... head `ca4d1d192`" refers to the screenshot-archive commit; the §7 fill-in landed later at `60d9022be` — internally consistent, no contradiction.
- **§8.2 Same-chain Behavior Witness** — open → `↓` next → `Esc` close documented as one event chain for both 1440 and 1920 implementation; post-close `body.overflow='visible'` + focus returned to the triggering chip is supported by `CalendarPeekProvider.tsx` close logic (F1 of W1 design). The prototype-side note (programmatic `__peek.open` has no native focus restoration, 1920 case primed `chips[0].focus()` first) is an honest, declared methodology caveat, not a fabricated PASS.
- **§8.3 Visual Drift Notes** — 3 declared drifts reviewed:
  1. Peek surface (right drawer → central modal): explicitly traced to §6 SSOT Conflicts row 2 (`Peek 浮卡新增`, lhr 2026-05-28). Valid.
  2. Prototype next-prev wraps across all demo chips vs impl scopes to mounted view: descriptive, consistent with W6 design; not a contract-intent drift. Valid.
  3. WeekCalendarView lacks `+N 更多`: confirmed by source (no overflow path in `WeekCalendarView.tsx`); reasoned via §6 row 5 copy unification + Month-only overflow. Disclosed. (See F2 for the §7 实现位置 annotation nit.)
- No **undeclared** §8.3 drift found: scanned §7 expectations vs source; the only nuance not in a drift row is the F1 command-string label, which is documentation cosmetic, not a behavior/visual divergence. No stop-and-report-to-lhr trigger.

## Commit Hygiene

`git log origin/main..HEAD --stat` — 5 atomic commits, correct type prefixes, no mixed concerns:

| Commit | Type | Files | Net |
| ------ | ---- | ----- | --- |
| `60414c801` land W1 visual contract | docs | contract only | +196 |
| `52b4f9065` land W1 review report | docs | `docs/reviews/sik-138-w1.md` only | +47 |
| `893c5c724` archive W7 a11y evidence | test | `axe-home-calendar.log` only | bin |
| `ca4d1d192` archive W7 chrome-mcp screenshots | docs | 16 PNGs only | bin |
| `60d9022be` close W7 acceptance hooks | docs | contract §7/§8 only | +58/-18 |

- No notion-workflow hotfix files leaked into the branch (hotfix lives on already-merged `origin/main` `4dfc057f5`).
- Each commit is one concern; no plan/schema/impl/test mixing. Within `<=15` files / `<=400` net per commit.

# Suggested Actions

- F1 (low): align §8.1 a11y command text with §7, or add a note that `npm run test:a11y` is the script alias. Non-blocking, can fold into a later doc touch.
- F2 (low): in §7 row 6, drop the `WeekCalendarView.tsx` 实现位置 reference or annotate "Month only" to match §8.3 row 3. Non-blocking.
- No corrective action required before Done; both are documentation-clarity nits with full traceability already present in §8.3.

# Risk Level

Low. All 16 acceptance hooks have concrete code/evidence backing; screenshots are complete, correctly named, valid PNGs at the explicitly-overridden archive path; a11y suite passes 13/13 exit 0; commits are atomic and clean; no undeclared visual drift. Residual: `0 high / 0 medium / 2 low`, both documentation-cosmetic.

> Note: this is a Reviewer-Mode审查通过, not a validation pass. Full `typecheck + lint + tests + browser smoke` validation is the Verifier's responsibility and out of scope for this document review.

# Decision

`review pass`
