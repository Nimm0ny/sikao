---
type: review
status: pass
owner: lhr
last-reviewed: 2026-05-30
issue: SIK-142
wave: W3
reviewer: independent subagent (Reviewer Mode)
target: SIK-142 W3 month/week cell overflow convergence
---

# SIK-142 W3 Review

## Scope

- Review target:
  - `apps/web/src/views/Home/sections/{MonthCalendarView,WeekCalendarView}.{tsx,module.css,test.tsx}`
  - `apps/web/src/views/Home/sections/dragDrop/MonthGridDnd.{tsx,test.tsx}`
  - `apps/web/src/views/Home/sections/calendarViewConfig/{factory.ts,factory.test.ts}`
  - Validation unblock only: `apps/web/src/views/EssayGradingResult.test.tsx`
- Contract baseline:
  - `docs/plan/sik-142-calendar-visual-unify-visual-contract.md` A11 / A12 / A13
  - W3 requirement: render all events, remove `+N 更多`, scroll inside month/week cell lists, hidden scrollbar, week default `cardLimitPerCell=3`

## Findings

### Initial Finding (Resolved Before Pass)

1. `F1` `High` — `cardLimitPerCell` had degraded into a dead semantic.
   Evidence:
   - Early W3 version used `cardLimitPerCell` only to mark `data-scrollable`, while month/week/DnD rendered all items without letting the config change real layout.
   Resolution:
   - Month, week, and month DnD list containers now all set `style.maxHeight` from `cardLimitPerCell`.
   - Month/Week/DnD tests were extended so custom `cardLimitPerCell=2` is asserted on the rendered list window, not only on a test hook.

### Open Findings

- None.

## Residual Risk

- Low: the visible-row height formula currently approximates chip height with `var(--space-6)`. Because month/week/DnD all share the same formula, drift would be consistent rather than fragmented, but if chip vertical geometry changes later this formula should be reviewed alongside `MonthEventChip`.

## Validation Evidence

- `npm run typecheck` — PASS
- `npm run lint` — PASS
  - Non-blocking pre-existing warnings remain in `apps/web/public/mockServiceWorker.js` and `apps/web/src/views/Home/sections/WeeklyReviewSection.tsx`
- `npm run test` — PASS
  - `@sikao/web` 108 files / 603 tests passed
- Scoped tests — PASS
  - Month / Week / MonthGridDnd / factory / EssayGradingResult targeted suite passed
- Browser smoke — PASS
  - Month default path: `selected=本月`, `gridcell=21`, `overflowChip=false`, `bodyOverflow=hidden`
  - Week default path: `selected=本周`, no overflow chip regression
  - Screenshot archive: `.tmp_review/visual-diff/sik-142/{w3-home-month-1440.png,w3-home-month-1920.png,w3-home-week-1440.png,w3-home-week-1920.png}`

## Conclusion

`review pass`.
