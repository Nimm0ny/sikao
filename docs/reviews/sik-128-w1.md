---
type: review
status: active
owner: codex
last-reviewed: 2026-05-28
issue: SIK-128
reviewer: independent-subagent
---

# SIK-128 · W1 Review

## 检查范围

- 当前未提交的 `SIK-128` Route A 相关改动
- `Workspace` / `RootLayout` / router 共享行为
- `AGENTS.md` / `CLAUDE.md` / H11 workflow / active V5 spec 文档同步
- `.tmp_review/visual-diff/sik-1920-cap/` 验收产物

本次审查为**独立只读 subagent review**。未修改任何业务文件。

## 发现项

### 1. High · nav baseline hook 被删除，H12 双重防线失效

- 文件：`D:/py_pj/sikao/.kiro/hooks/nav-baseline-guard.kiro.hook`
- 证据：
  - 工作区当前状态为删除：`git status --short`
  - H12 明文要求 “Steering `.kiro/steering/nav-baseline.md` + Hook `nav-baseline-guard` 双重防御”
  - 规则位置：[AGENTS.md](D:/py_pj/sikao/AGENTS.md:89)
- 影响：
  - 这不是 Route A 必需改动
  - 会直接削弱 nav baseline 防呆
  - 在当前规则下属于 blocker
- 建议：
  - 若这是误删，恢复该 hook
  - 若这是用户有意移除，必须同步改写 H12 和相关 gate 文档，不能一边删 hook 一边保留 “双重防御” 规则

### 2. High · H11/H8 验收证据在审查时未闭环

- 文件：
  - [sik-128-workspace-dashboard-visual-contract.md](D:/py_pj/sikao/docs/plan/sik-128-workspace-dashboard-visual-contract.md:160)
  - `D:/py_pj/sikao/.tmp_review/visual-diff/sik-1920-cap/`
- 证据：
  - 审查时目录里只有 `implementation-*` PNG，没有 `prototype-*`
  - summary 里还记录了 `/profile/learning` 相关 chart-size warning
- 影响：
  - 当时最多只能说“实现截图已归档，prototype 对照与 warning 处置未收口”
- 当前状态说明：
  - 本发现项在审查时成立
  - 后续若已补 `prototype-*` 截图与 warning 定性，需在 Evidence Block 里显式回填“审查后已处理”

### 3. Medium · 共享改动的真实影响面大于当前 requirement 列表

- 文件：
  - [router/index.tsx](D:/py_pj/sikao/apps/web/src/router/index.tsx:47)
  - [requirements.md](D:/py_pj/sikao/.kiro/specs/sik-128-workspace-dashboard-cap/requirements.md:21)
  - [sik-128-workspace-dashboard-visual-contract.md](D:/py_pj/sikao/docs/plan/sik-128-workspace-dashboard-visual-contract.md:32)
- 证据：
  - `RootLayout` 下不只 8 个入口 route，还包括 `practice/mock-exam/*` 与 `/profile/practice-preferences`
  - Route A 改的是共享 `Workspace` 默认值
  - 当前 requirement / contract 没给这些非 8 路由明确 owner
- 风险：
  - 留下“共享行为已变，但非 8 路由未验也未挂 follow-up owner”的空档
- 建议：
  - 要么补 smoke / 截图
  - 要么在 contract / issue 明确 follow-up owner 与 issue

### 4. Medium · 文档真相曾经不同步，容易误导后续 runner/reviewer

- 文件：
  - [design.md](D:/py_pj/sikao/.kiro/specs/frontend-style-guide-v5/design.md:458)
  - [design.md](D:/py_pj/sikao/.kiro/specs/frontend-style-guide-v5/design.md:1198)
  - [design.md](D:/py_pj/sikao/.kiro/specs/frontend-style-guide-v5/design.md:1274)
  - [evidence.md](D:/py_pj/sikao/.kiro/specs/frontend-style-guide-v5/evidence.md:95)
  - [08-Question-Hub-Page.md](D:/py_pj/sikao/docs/vault/05-migration/Phase/Review/08-Question-Hub-Page.md:12)
  - [frontend-tab-runtime-2026-05-24.md](D:/py_pj/sikao/docs/plan/frontend-tab-runtime-2026-05-24.md:541)
- 证据：
  - 审查时 `.kiro` V5 spec 仍残留 5-tab / 题库文案
  - Review / runtime plan 仍残留 `/q/:id`
- 风险：
  - 后续实现可能再次按旧文档回滚到错误 nav / route
- 建议：
  - 统一到当前 4-tab + `/question-hub` 真相
  - 审查后如已修，需在 close-out 里注明“文档残影已处理”

## 风险等级

- `HIGH`

理由：

- 有 2 个 high，其中 nav hook 删除是当前仍然存在的 blocker
- 其余是中风险一致性问题，会放大后续误判概率

## 建议处理

1. 恢复 `.kiro/hooks/nav-baseline-guard.kiro.hook`，或明确改写 H12 以取消 hook 防线
2. 把 prototype 截图与 warning 定性补齐后，回填到 Evidence Block
3. 给 `practice/mock-exam/*` 与 `/profile/practice-preferences` 补 owner 或补 smoke
4. 把 5-tab / `/q/:id` 文档残影统一到当前真相

## Residual Risk

- Home `live-expanded-metrics.json` 中的 `rightGutter: 15` 未直接定性为 bug，建议在下一轮 browser 验收时顺手复核 Home 是否仍出现整页滚动条
- 现有 `Workspace.test.tsx` 只锁 `data-max-width` attr，不锁真实渲染宽度；真实宽度仍主要依赖 browser smoke 证明
