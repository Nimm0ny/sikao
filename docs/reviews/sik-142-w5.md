---
type: review
status: pass
owner: lhr
last-reviewed: 2026-05-30
issue: SIK-142
wave: W5
reviewer: independent subagent (Reviewer Mode)
target: SIK-142 W5 week chip readonly peek + peek V5 alignment
---

# SIK-142 W5 Review

## Scope

- Review target:
  - `apps/web/src/views/Home/sections/WeekCalendarView.{tsx,test.tsx}`
  - `packages/design-system/src/tokens.css`
  - `apps/web/src/views/Home/sections/peek/CalendarPeekCard.module.css`
  - Read-only review target (existing peek surface): `peek/{CalendarPeekCard,CalendarPeekHead,CalendarPeekProperties,CalendarPeekNotes,CalendarPeekProvider,types}.tsx`
- Contract baseline:
  - `docs/plan/sik-142-calendar-visual-unify-visual-contract.md` A17 / A18
  - W5 requirement: week chip opens existing read-only Peek, and source / link / target channels remain visible in Peek

## Findings

### Initial Findings (Resolved Before Pass)

1. `F1` `High` — week chip wiring overwrote real `event.id` with the scoped peek id.
   Resolution:
   - `WeekCalendarView.tsx` now preserves the original event id and passes the scoped id only through `peekAnchorId`.

2. `F2` `Medium` — `peekList` ordering was not chronological.
   Resolution:
   - Week peek entries now collect by `day -> slot`, then sort by `startAt`, `endAt`, and id.
   - Test now locks `Monday noon -> next -> Monday evening`.

3. `F3` `Medium` — Peek radius tokens referenced nonexistent variables.
   Resolution:
   - `--cal-peek-radius` now references `--card-radius`.
   - Peek button / banner / kind bar radii now use valid V5 tokens.

### Open Findings

- None.

## Residual Risk

- Low: `CalendarPeekHead.tsx` still uses `lucide-react` icons. That remains an older surface-level inconsistency with the stricter sprite-only interpretation of the design system, but it was pre-existing and is not newly introduced by W5.

## Validation Evidence

- `npm run typecheck` — PASS
- `npm run lint` — PASS
  - Non-blocking pre-existing warnings remain in `apps/web/public/mockServiceWorker.js` and `apps/web/src/views/Home/sections/WeeklyReviewSection.tsx`
- `npm run test` — PASS
  - `@sikao/web` 107 files / 608 tests passed
- Scoped tests — PASS
  - `WeekCalendarView.test.tsx`, `CalendarPeekCard.test.tsx`, `Home.test.tsx`, `CalendarPanel.test.tsx`
- Browser smoke — PASS
  - week tab selected, chip click opens peek, readonly banner visible, source/target/linked rows present
  - Screenshot archive: `.tmp_review/visual-diff/sik-142/{w5-home-week-1440.png,w5-home-week-peek-1440.png}`

## Conclusion

`review pass`.
