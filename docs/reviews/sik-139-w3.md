# SIK-139 Wave 3 — Independent Review (落点冲突校验 + 二次确认弹层)

> Reviewer Mode（独立 subagent session，只读，未改任何代码）。
> 触发：H5（跨写边界 wave + 前端视觉 phase 必须独立 subagent review）。
> 被审范围：本地 main `ff86e7283..1ee90e5dd`（5 commit，未 push origin，等本 review pass）。

## 检查范围

Commits（oldest→newest）：

- `73e4665e4` feat(api-client): re-export ProposedPlanEventV2 + EventConflictItemV2 for SIK-139 W3
- `1585bf9b5` feat(home-calendar): SIK-139 W3 conflict guard + proposed event builder (pure, H7 fail-fast)
- `345c25bc1` feat(home-calendar): SIK-139 W3 conflict gate orchestrator (clear/conflict/error split, H7)
- `b37b399ce` feat(home-calendar): SIK-139 W3 conflict confirm dialog (portal + FocusTrap + Esc, H11 tokens)
- `1ee90e5dd` feat(home-calendar): SIK-139 W3 wire conflict gate into month drop path

文件：

- 新增 `apps/web/src/views/Home/sections/dragDrop/`：`conflictGuard.ts`(+test) / `proposedEvent.ts`(+test) /
  `runConflictGate.ts`(+test) / `ConflictConfirmDialog.tsx` + `.module.css`(+test)
- 改动 `dragDrop/MonthGridDnd.tsx`(+test) / `MonthCalendarView.tsx` / `dragDrop/resolveCalendarDrop.ts`
- 改动 `packages/api-client/src/types/home.ts`（re-export）

## 逐条核查（7 点）

| # | 项 | 结论 | 证据 |
|---|---|---|---|
| 1 | **H7 红线：detect 请求失败 vs 有 conflicts 绝不混淆** | PASS | `conflictGuard.ts` `checkDropConflicts` 不 try/catch，失败原样抛（无吞）；`runConflictGate.ts` catch 后立即 `deps.onError(...)` + `return`，不 fall through 到 `onClear`（决策层第一道闸）；`MonthGridDnd.tsx` `onError` 分支仅 `toast.error` 不提交。有 conflicts 走 `onConflict` → 开弹层不自动提交。 |
| 2 | **取消路径无副作用** | PASS | `MonthGridDnd.handleConflictCancel` 仅 `setPendingConflict(null)`；弹层 Esc/scrim/cancel 统一路由 `onCancel`，不写 store、不发 PATCH、不动 W2 commit。 |
| 3 | **复用 W2 不重造** | PASS | `commitReschedule` / `rescheduleEvent` / `useRescheduleEvent` 本 Wave 未改（`git diff --name-only` 无这三文件）；clear 路径与 confirm 路径都走同一 `runCommit`（包 `commitReschedule`），无第二份提交逻辑。`resolveCalendarDrop` 仅扩展可选字段，决策分支不变。 |
| 4 | **类型纪律** | PASS | 4 个新文件 grep `\bany\b` / `as any` / `@ts-ignore` 0 命中；`npm run typecheck` Exit 0。W2 既有的 `active.data.current as DropDragData` 断言非本 Wave 引入。 |
| 5 | **H6 窗口边界正确性** | PASS | 后端 `ExistingWindowV2.from/to` 为 `date`；`buildViewRange` 返回 UTC ISO datetime。`conflictGuard.zonedDateKey` 用 `Intl.DateTimeFormat('en-CA', { timeZone })` 把 UTC 实例映射到 Asia/Shanghai 日历日，避免 +08:00 跨界差一天；与后端 `_build_utc_window` 自洽。unparseable 实例抛错无 fallback（H7）。 |
| 6 | **H11 token 单源** | PASS | `ConflictConfirmDialog.module.css` 全部值走 tokens.css 既有 token（`--cal-peek-scrim` / `--shadow-l4` / `--cal-peek-radius` / `--card-bg` / `--color-state-*` 等），无原型 var 直写、无 color-mix 硬编码、无 literal box-shadow。按钮复用 `Button` 组件（variant=secondary/danger）。 |
| 7 | **测试充分性（TDD 三分支）** | PASS | 无冲突/有冲突/请求失败三分支在 `conflictGuard.test` + `runConflictGate.test` 全覆盖；弹层渲染/a11y/按钮/Esc/scrim + vitest-axe 0 violation 在 `ConflictConfirmDialog.test`。dragDrop 9 files / 47 tests 全过。 |

## 发现项

| 编号 | 严重度 | 证据 | 建议处理 |
|---|---|---|---|
| F-1 | Info | `MonthGridDnd` `conflicts={pendingConflict?.conflicts ?? []}` | 成功路径防御默认值，detect 失败仍走 onError fail-fast，不构成 silent fallback。保留。 |
| F-2 | Info | `buildProposedEvent` `category: data.category ?? ''` | 拖拽 data 必带 category（chip 来自 PlanEventReadV2），`?? ''` 仅满足 wire 必填，非业务 fallback。保留。 |
| F-3 | Low | gate 接线（onConflict→弹层 / onConfirm→runCommit / 取消→无副作用）缺**组件级**集成测试 | 与 W2 review M-1 取舍一致：逻辑已抽 `runConflictGate`(决策三分支) + `commitReschedule`(提交两分支) + dialog(渲染) 各自单测；grid 仅声明式 wiring，由 typecheck 保证。真实 drop→弹层→确认/取消 留 Verifier browser smoke。建议 Wave 4 a11y 键盘改期落地后补一条组件级集成。 |
| F-4 | Info | `MonthGridDnd.tsx` / `MonthCalendarView.tsx` grid/gridcell ARIA 嵌套 diagnostics | W1/W2 既有债（W1 review Low / W2 review M-2），已归 Wave 4，非本 Wave 引入，非 blocker。 |

无 High / 无 Medium。

## 自跑验证实证（apps/web）

- `npm run typecheck` → Exit 0
- `npx vitest run src/views/Home/sections/dragDrop/` → 9 files / 47 tests passed
- `node scripts/lint-hardcode.mjs` → 0 violations
- `node scripts/lint-shadow-token.mjs` → clean

## 风险等级

低。新增为前置 gate（W2 编排零改动，可独立 revert）；fail-fast 三分支严格隔离；token 单源。

## 结论

**review pass**（0 High / 0 Medium；F-3 Low 与 W2 取舍一致，建议 Wave 4 收口）。

## 流程提醒（非本 review 范围，收口前补）

- H8 UI browser smoke（18080 实机：拖拽→冲突→弹层→确认/取消，1440/1920 双开）须由 Verifier 补，Runner 未伪造。
- H11 视觉契约 §7「冲突确认层」行 + 1440/1920 双开 diff 截图归档由 Verifier closeout。
