# SIK-143 · Home 底栏 4 卡等宽收口 视觉契约 W1 独立审查 (2026-05-30)

> Reviewer Mode（独立视觉规范审查官，只读，未改任何源码 / 未改契约文件）。
> 被审对象：`docs/plan/sik-143-home-bottomrow-density-visual-contract.md`（H11 视觉契约，Define-First）。
> 审查触发源：`docs/reviews/sik-fu-d-bottomrow-density-w1.md`（2026-05-30 W1 审查 → changes-required → 本契约即收口产物）。
> 结论先行：**pass**（七段齐全且实质，关键事实与源码/原型/token 一致；无 P0/P1。
> 余下 1 项 P2 + 多项 P3 为文档同步/可追溯性改进，不阻塞 Runner，但建议 issue 收尾前处理）。

## 0. 检查范围

### 0.1 被审文档

- `docs/plan/sik-143-home-bottomrow-density-visual-contract.md`（全文 8 段 + front-matter）

### 0.2 交叉核对的 SSOT / 源码（只读对照，验证契约事实正确性）

- 原型：`.tmp_review/out/Tab1-Home/Home v2.1.html`（`.bottom-row` L1030-1036、`.bottom-card` L1036-1047、`.bc-head` L1047-1059、`.weak-list` L1101-1104、`.feed-list` L1134-1140、`.icon-btn` L390-401、DOM L1450-1540）
- 生产源码：
  - `apps/web/src/views/Home/Home.tsx`
  - `apps/web/src/views/Home/Home.module.css`
  - `apps/web/src/views/Home/sections/RecommendationSection.{tsx,module.css}`
  - `apps/web/src/views/Home/sections/ProgressSection.{tsx,module.css}`
  - `apps/web/src/views/Home/sections/WeeklyReviewSection.module.css`
  - `apps/web/src/views/Home/sections/RecentPracticeSection.module.css`
  - `apps/web/src/views/Home/sections/MetricRow.module.css`（9999px 全量核对用）
  - `apps/web/src/components/layout/Panel/Panel.tsx`
  - `packages/design-system/src/tokens.css`（radius / card token）
- 规范 SSOT：`docs/vault/04-design/Web-Layout.md` §1/§5、`docs/vault/04-design/Prototype-Token-Map.md` §5/§8/§12

### 0.3 能力边界声明（H3）

- shell / 文件读 / grep：可用，已用于行号与裸值核对。
- Notion MCP：可用，但本次任务限定为「契约文档审查」，**未 fetch SIK-143 Notion issue**；故 H11
  「契约被 issue `## Acceptance` 显式引用」一项无法在本审查内签发，记为 UNVERIFIED（见 F6）。
- Chrome MCP 双开实测：契约 §7.2 的 1440/1920 截图属 Runner 落地后产物，本审查不涉及运行时实测。

## 1. 维度一 · H11 七段完整性

逐段核对「在 + 实质（非空壳）」：

| H11 段 | 契约对应 | 在? | 实质? | 备注 |
|---|---|---|---|---|
| 1 Layout Topology | §1（1.1 root grid / 1.2 rightStack / 1.3 一屏 lock / 1.4 子区域 owner） | ✅ | ✅ | 给出目标 CSS + minmax(0,*) 理由 + ScreenLockShell rows + 局部滚链路 |
| 2 Required Interactive Elements | §2 + §2.1 刷新 icon-btn 规格 + §2.2 testid/aria-label | ✅ | ✅ | 4 卡控件清单 + a11y 替代方案明确 |
| 3 Information Density | §3（每卡视觉块数 + 视觉编码表） | ✅ | ✅ | 含局部滚 / overflow 收口说明 |
| 4 Token Map | §4 + §4.1 4 处修复 + §4.2 非 token 字号登记 | ✅ | ✅ | 引用 Prototype-Token-Map，before/after 表完整 |
| 5 SSOT Conflicts | §5（3 行冲突 + 采用方 + lhr 拍板日期） | ✅ | ✅ | 列比例 / 卡容器 / 双标题三冲突均含 2026-05-30 拍板 |
| 6 Visual Drift | §6（3 行 drift + 偏离原因 + 拍板日期） | ✅ | ✅ | 底栏结构 / 列比例 / 刷新入口；末尾 no-further-drift 收口 |
| 7 Acceptance Hooks | §7.1 对照表（A1-A10）+ §7.2 1440/1920 截图归档路径 + 量表证据 | ✅ | ✅ | 含实现 vs 原型对照 + 双开 diff 路径 + scrollHeight 量表口径 |

判定：**七段全在且实质，无空壳段**。H11 硬门禁满足。

## 2. 维度二 · Token Map 正确性

- **引用 Prototype-Token-Map.md**：§4 首行明确「引用 `docs/vault/04-design/Prototype-Token-Map.md`（H11 强制查表）」。✅
- **9999px → `--radius-999` 映射登记**：§4.1 表把 4 处裸 `9999px` 一一登记为 `var(--radius-999)`。
  逐条核对源码裸值确实存在：
  - `ProgressSection.module.css` barTrack `border-radius: 9999px`（实测 L67）✅
  - `ProgressSection.module.css` barFill `border-radius: 9999px`（实测 L75）✅
  - `WeeklyReviewSection.module.css` streakPill `border-radius: 9999px`（实测 L67）✅
  - `RecentPracticeSection.module.css` feedPill `border-radius: 9999px`（实测 L100）✅
  - token 侧 `--radius-999: 9999px`（`tokens.css:335`）确认存在，映射方向正确。✅
- **非 token 字号集中登记**：§4.2 列了 10.5 / 10 / 9.5 / 11.5px 五项，逐条核对源码：
  - ProgressSection weakVal `10.5px`（L85，带 spacing-allow）✅
  - WeeklyReviewSection streakPill `10px`（L63）/ dotLabel `9.5px`（L115）✅
  - RecentPracticeSection feedName `11.5px`（L83）/ feedSub `10px`（L92）/ feedPill `10px`（L97）✅
  - §4.2 末尾「本次不新增非 token 字号，仅集中登记既有项」与现状一致（这些字号当前已在 main，带 spacing-allow）。✅

判定：Token Map 段事实正确，映射登记完整。仅 1 处注释表述需修正（见 F1，P2，非阻塞）。

## 3. 维度三 · SSOT Conflicts 登记

§5 表第 1 行：

> 底栏列比例 | SIK-127 `1.4fr 1.2fr 1fr`（裸 fr，左二不等宽） | `minmax(0,1.4fr) minmax(0,1.4fr) minmax(0,1fr)`（左二严格等宽） | **B（本次）** | 2026-05-30

- 核对 SIK-127 现状：`Home.module.css:13` `grid-template-columns: 1.4fr 1.2fr 1fr`，裸 fr，左二不等宽 ✅
- 本次目标 `minmax(0,1.4fr) minmax(0,1.4fr) minmax(0,1fr)` 与 §1.1 root grid 代码块一致 ✅
- 含 lhr 拍板日期 2026-05-30 ✅
- 第 2/3 行（左二卡容器 Panel→bottomCard、双标题消除）同样含采用方 + 拍板日期 ✅
- §5 末尾要求同步 Web-Layout.md §5 owner 表（bottomRow 行标注 SIK-143 supersede SIK-127），并注明
  「文档同步在本 issue 收尾或后续 doc commit 处理，不阻塞实现」——与 H6/H11 精神一致（边界已登记，落地分提交）。✅

判定：列比例冲突 SIK-127(1.4/1.2/1fr) vs 本次(1.4/1.4/1fr) + lhr 2026-05-30 拍板，**已正确登记**。维度三满足。

## 4. 维度四 · Visual Drift from Prototype

§6 表第 1 行：

> 底栏结构 | 原型 `.bottom-row` 是 **3 等宽卡**（`1fr 1fr 1fr`），无 right-stack | 沿用 SIK-127 right-stack 扩展 + 左两列等宽 | ... | 2026-05-30

- 核对原型：`Home v2.1.html:1033` `grid-template-columns: 1fr 1fr 1fr`（三等宽）✅；DOM L1450 注释
  `BOTTOM ROW · 3 cards`，三张 `.bottom-card` 平铺（L1453/1479/1503），**无 right-stack 容器** ✅
- 「right-stack 是 SIK-127 对 v2.1 的既有扩展（已认定）」与 W1 审查（sik-fu-d-bottomrow-density-w1.md §2 判定1）
  口径一致 ✅
- 含 lhr 拍板日期 2026-05-30 ✅
- 第 2 行（列比例 1fr×3 → 1.4/1.4/1fr）、第 3 行（刷新入口 drift）同样含偏离原因 + 拍板日期 ✅
- 末尾「除上述登记项外…no further drift」收口 ✅

判定：原型 3 等宽无 right-stack vs 本实现 right-stack 扩展 + 拍板日期，**已正确登记**。维度四满足。

## 5. 维度五 · Acceptance Hooks

- **实现 vs 原型对照表**：§7.1 给出 A1-A10 共 10 行，每行含「项 / 原型基准（含原型 HTML 行号）/ 实现位置 /
  验收勾选」四列。✅ 覆盖列等宽+overflow（A1）、右栈等高（A2）、bottomCard+bc-head 去 57px header（A3）、
  消双标题（A4）、刷新 icon-btn（A5）、局部滚（A6）、4 处 9999px→token（A7）、死代码删除（A8）、
  1920/1440 一屏不溢出（A9/A10）。与 issue 修复目标一一对应。✅
- **1440/1920 双开 diff 截图归档路径**：§7.2 明确 `.tmp_review/visual-diff/sik-143/` +
  `{prototype,implementation}-desktop-{1440x900,1920x1080}.png` 四张。✅ 满足 H11 第 7 段「1440/1920
  Chrome MCP 双开 diff 截图归档路径」要求。
- **量表证据口径**：§7.2 给出 `ScreenLockShell.scrollHeight <= clientHeight`、`document...scrollHeight <=
  clientHeight`、4 卡 `getBoundingClientRect().bottom <= bottomRow.bottom` 三条可执行量表。✅ 比 W1 仅
  「未执行」更进一步，给了 Runner 可直接复现的验收脚本口径。

判定：含对照表 + 双开 diff 归档路径 + 量表口径，**维度五满足**。

## 6. 维度六 · 与上游 SSOT 一致性

- **Web-Layout §1 一屏锁死**：契约 §1.3 复述「Home 由 ScreenLockShell rows="auto auto minmax(0,1.6fr)
  minmax(0,1fr)" 包裹（不变，H12 范畴外）」「底栏整行 = 第 4 行 minmax(0,1fr) lock，不整页滚」
  「卡内列表局部滚」。核对 `Home.tsx:51` rows 串一字不差；新增 `.bottomRow{overflow:hidden}` 是对一屏
  锁死的**强化**（裁在 row 内），与 §1 不冲突。✅
- **Web-Layout §5 owner 表**：现表 bottomRow #1=SIK-FU-A / #2=SIK-91 / #3=SIK-92。契约 §1.4 子区域 owner
  与 §5 拍板「SIK-143 owner 第 4 行内部 3 列分配，supersede SIK-127 列口径」并在 §5 末尾要求回写 owner 表。
  这是**有意的 owner 边界变更**，已按 H11/H1 显式登记 + lhr 拍板，不是静默越权。✅
  （注：§5 owner 表当前文案把 #3 标 Recommendation owner=SIK-92、#2 Progress owner=SIK-91，与契约 §1.4
  的「卡头收口 owner=SIK-143 / 业务 owner=SIK-91/92」分层口径一致，无矛盾。）
- **Prototype-Token-Map §12 反模式**：§12 红线「圆角必须走 token，裸值是红线」「禁非 token 比例，特殊比例
  必须在 visual-contract 说明」。契约 §4.1 把 4 处裸 9999px 收敛到 token（消红线）；§1.1 的 1.4/1.4/1fr
  非标准比例在契约内显式说明（满足 §12「特殊比例必须在 visual-contract 里说明」豁免条件）。✅

判定：契约结论与 Web-Layout §1/§5、Prototype-Token-Map §12 **一致，无冲突**。维度六满足。

## 7. 维度七 · 事实错误 / 自相矛盾 / 引用行号

逐项核对契约内所有可证伪断言：

- §0 「左二卡用无收口 `<Panel>` 一屏溢出」「57px Panel header + 双重标题」：核对 `Home.tsx:61,65`
  左二卡确为 `<Panel title>`；`ProgressSection.tsx:96` 内层 `<h4>Top 3 弱项</h4>` + Panel title「学习进度」
  = 双标题，属实。✅
- §0/§4.1 「4 处裸 9999px」：核对源码确为 4 处（Progress×2 + Weekly×1 + RecentPractice×1）。✅
  ⚠ 但全仓另有第 5 处 `MetricRow.module.css:104 border-radius:9999px`，见 F2（契约 Scope 未含 MetricRow，
  不算错误，但「4 处」是「本契约 Scope 内 4 处」而非「全 Home 4 处」，建议措辞收紧）。
- §0 「`Home.module.css` 死代码」：grep `taskList|taskItem|recommendList|recommendItem|reviewSummary|
  reviewBadges|taskTitle|taskStatus|recommendTitle|recommendMeta` 在 `apps/web/src/**/*.tsx` **0 命中**，
  证实 `Home.module.css:50-127` 这批 class 全无引用 = 死代码，属实。✅
- §2.1 「sprite 无 refresh 图标，用 inline SVG」「lint-icon-style.mjs 只扫 .svg 不扫 tsx inline svg」：
  属设计决策声明，方向合理（与 WeeklyReview ring inline svg 同例）；本审查未深挖 lint 脚本实现，记为
  合理假设（F4，P3 建议 Runner 落地时验证 lint-icon-style 实际扫描范围）。
- §2.2 「4 卡都保留 data-testid="panel"」：核对 `Panel.tsx` title 分支输出 `data-testid="panel"`；
  `Home.tsx` 右栈两卡 `<section data-testid="panel">` 已硬编码。左二卡改 bottomCard 后需手动补 testid——
  契约 §0 改动对象已写明「4 卡 data-testid="panel"」，逻辑自洽。✅
- §7.1 原型行号：A1 `.bottom-row` L1030-1036 ✅、A3 `.bottom-card` L1036-1046 / `.bc-head` L1047-1059 ✅、
  A5 `.icon-btn` L390-401 ✅、A6 `.feed-list` L1134-1140 / `.weak-list` L1101-1104 ✅、A4 学习进度卡
  「`.bottom-card` 第 2 格 L1479-1502」核对原型 L1479 起第 2 张 bottom-card（Top 3 弱项）✅。引用行号全部命中。
- §4 token 注释 `--radius-999（9999px）`：见 F1——Prototype-Token-Map §5 把 `--r-pill→--radius-999` 标值
  `999px`，而 tokens.css 实际 `--radius-999: 9999px`。契约写 9999px **与生产 token 真值一致（正确）**，
  反而是 Prototype-Token-Map §5 的 `999px` 与 tokens.css 不符。契约不背锅，但建议在契约 §4 加一行脚注
  指出该上游 drift，避免 Runner 困惑（F1，P2）。

判定：**无致命事实错误、无自相矛盾、引用行号全部正确**。仅措辞/可追溯性可优化（F1/F2）。

## 8. 发现项汇总（编号 · 严重度 · 证据行号）

| 编号 | 严重度 | 标题 | 证据 |
|---|---|---|---|
| F1 | P2 | 上游 drift：Prototype-Token-Map §5 `--r-pill→--radius-999` 标值 `999px`，但 `tokens.css:335` 实为 `9999px`。契约 §4 写 `9999px` 与生产 token 真值一致（契约正确），但与所引用的 Token-Map 表面数值打架，Runner 查表会困惑 | `Prototype-Token-Map.md` §5 `999px` vs `tokens.css:335 --radius-999:9999px`；契约 §4 表注 `--radius-999（9999px）` |
| F2 | P3 | 契约「4 处裸 9999px」实为「Scope 内 4 处」；全仓另有第 5 处 `MetricRow.module.css:104`（不在 Scope，不算错），建议措辞改为「本契约 4 个 section 内 4 处」避免被误读为「全 Home 仅 4 处」 | `MetricRow.module.css:104 border-radius:9999px`；契约 §0/§4.1「4 处」 |
| F3 | P3 | H11 闭环未在本审查内确证：契约需被 SIK-143 Notion issue `## Acceptance` 显式引用方满足 H11；本次未 fetch Notion issue，记 UNVERIFIED，Runner/Master 须补验 | 任务边界（未授权 Notion fetch）；H11 第 7 段 + If-conflict |
| F4 | P3 | §2.1 「lint-icon-style.mjs 只扫 .svg 不扫 tsx inline svg」「sprite 无 refresh 图标」为合理假设，本审查未核对 lint 脚本与 sprite 清单；Runner 落地刷新 icon-btn 前应实测 lint 不报错 | 契约 §2.1；未读 `lint-icon-style.mjs` / sprite manifest |
| F5 | P3 | §5 要求回写 Web-Layout §5 owner 表（bottomRow supersede SIK-127），声明「不阻塞实现」。合规，但需确保 issue 收尾确有该 doc commit，否则 §5 owner 表与实现长期 drift | 契约 §5 末段；`Web-Layout.md` §5 现表无 SIK-143 行 |
| F6 | P3 | §6 drift 表第 3 行称刷新「从浮底 Button 上移到 bc-head 更贴原型 icon-btn 模式」；原型对应位（最近练习 feed）本无刷新按钮，属 SIK-92 语义新增。表述无错（偏离原因列已注明 SIK-92），仅提示这是「新增控件」而非「移动既有原型控件」，验收别误按原型找基准 | 契约 §6 第 3 行；原型 L1503 起第 3 卡为「最近练习」无 refresh |

> 严重度口径：P0=阻断 / P1=必须改才能 Runner / P2=应改但不阻塞 / P3=可追溯性·措辞·建议。
> 本次**无 P0/P1**。F1 为唯一 P2（且根因在上游 Token-Map，非契约本身错误）。

## 9. 建议处理

1. **F1（P2，建议契约收尾顺手加脚注）**：在契约 §4 token 表加一行脚注，例如
   「注：Prototype-Token-Map §5 标 `999px` 系上游笔误，生产 `tokens.css` 真值为 `9999px`，本契约以
   生产 token 为准」。同时建议另开微 doc 任务修 `Prototype-Token-Map.md` §5 的 `999px`→`9999px`
   （超本契约 Scope，单独 commit）。
2. **F2（P3）**：§0 与 §4.1「4 处」改为「本契约 4 个 section 内 4 处」，杜绝「全 Home 仅 4 处」误读。
3. **F3（P3，Runner/Master 必办）**：开 Runner 前确认 SIK-143 Notion issue `## Acceptance` 已显式引用
   `docs/plan/sik-143-home-bottomrow-density-visual-contract.md`，否则按 H11 issue 视为未完成。
4. **F4（P3，Runner 落地时）**：实现刷新 icon-btn 后跑 `lint-icon-style` + `lint-icon-button`，确认 inline
   SVG 不触发 sprite 检查、aria-label 满足 icon-only 规则。
5. **F5（P3，issue 收尾）**：补一笔 doc commit 回写 Web-Layout §5 owner 表 bottomRow 行（SIK-143 supersede
   SIK-127 列口径），避免 owner 表与实现 drift。
6. **F6（P3）**：可在 §6 第 3 行加「（新增控件，非原型既有）」一词，纯措辞，验收更清晰。

## 10. 风险等级

- **整体：低**。契约七段齐全且实质，关键事实（4 处裸值、双标题、Panel header、死代码、原型 3 等宽无
  right-stack、列口径冲突、token 映射）逐条与源码/原型/token 核对**全部属实**，引用行号准确，无自相矛盾。
- 数据 / 鉴权 / DB / API 契约：无关，零风险。
- 唯一 P2（F1）根因在上游 Token-Map 数值笔误，契约本身取值正确，不构成实现风险。
- 残留风险集中在「文档闭环 / 可追溯性」（F3 H11 引用闭环、F5 owner 表回写），属流程门禁，非契约质量问题。

## 11. 总体结论

**pass**。

- 契约满足 H11 七段 Define-First 硬门禁，可作为 SIK-143 Runner 的验收 SSOT。
- 准入条件（开 Runner 前）：办妥 F3（Notion issue `## Acceptance` 显式引用本契约）。这是 H11 的
  issue 闭环要求，与契约文档质量无关，但不补则 issue 视为未完成。
- 建议（不阻塞）：F1 加上游 drift 脚注、F2 措辞收紧、F4 Runner 落地实测 icon lint、F5 收尾回写 owner 表。
- 本审查仅审「契约文档」，**未审一笔实现 diff**（实现尚未落地）；Runner 落地后须按 §7.1 A1-A10 +
  §7.2 1440/1920 Chrome MCP 双开 diff 另做实现态视觉审查（H5/H11），不能以本契约审查 pass 代替实现验收。

---
Reviewer: Kiro（独立视觉规范审查官 / Reviewer Mode）
Date: 2026-05-30
Scope basis: 契约 `sik-143-home-bottomrow-density-visual-contract.md`；交叉核对 main 源码 5 文件 + Panel.tsx
+ tokens.css + 原型 Home v2.1.html；上游 Web-Layout.md §1/§5 + Prototype-Token-Map.md §5/§12
