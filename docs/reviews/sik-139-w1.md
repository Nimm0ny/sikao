---
type: review
status: complete
mode: Reviewer
owner: lhr
reviewer: subagent-independent (Reviewer Mode)
last-reviewed: 2026-05-29
notion-issue-url: https://www.notion.so/36fbc174f6c881ee9e7bdae2a9e3b29a
notion-issue-identifier: SIK-139
wave: W1
verdict: review pass
---

# SIK-139 Wave 1 独立 Review — dnd-kit DndContext + sensors 接入月视图（drop = no-op）

> Reviewer Mode（只读审查）。依据 `AGENTS.md` H1-H12，本次为 H5/H11 触发的独立
> review（前端视觉 phase + 触及交互/事件层）。结论基于独立 session 自跑的
> typecheck / eslint / vitest / token linter 证据，不只信报告。

## 1. 检查范围

分支：`feat/sik-139-w1-dndcontext`（相对 base `origin/main` c60ab5a69，4 个 commit）。

| Commit | 类型 | 内容 |
|---|---|---|
| `0d37e82c2` | feat | add --cal-drag-* tokens for SIK-139 W1 dragging chip |
| `799e2153c` | feat | wire dnd-kit DndContext + sensors onto month view（含 W0 F-2 注释） |
| `78eb92c7c` | feat | add dragging chip visual state（CSS） |
| `701ee2b64` | test | cover dnd-kit wiring + no-op drop |

改动文件（`git diff --stat origin/main..HEAD`，+567 / -9，8 文件）：

- `packages/design-system/src/tokens.css`（+10）
- `apps/web/src/views/Home/sections/dragDrop/useCalendarDragSensors.ts`（+48）
- `apps/web/src/views/Home/sections/dragDrop/useCalendarDragSensors.test.tsx`（+38）
- `apps/web/src/views/Home/sections/dragDrop/MonthGridDnd.tsx`（+233）
- `apps/web/src/views/Home/sections/dragDrop/MonthGridDnd.test.tsx`（+152）
- `apps/web/src/views/Home/sections/MonthEventChip.tsx`（+33 / -? 附加）
- `apps/web/src/views/Home/sections/MonthEventChip.module.css`（+18）
- `apps/web/src/views/Home/sections/MonthCalendarView.tsx`（+44）

SSOT 对照基准：plan §4/§7、spec requirements（R5 a11y / R6 fail-fast / R9 token）+
design Decisions、visual-contract §2/§4/§7。

## 2. 逐条核查结论（9 项全 PASS）

1. **Scope discipline（Wave 1 = no-op）— PASS**
   `handleDragEnd` 仅 `setActiveDragId(null)`；`dragDrop/**` 运行时代码 grep
   `useUpdateEvent` / `upsertOptimisticEvent` / `mutateAsync` / `detectEventConflicts`
   **0 命中**（仅注释 + 测试）。`optimisticEvents` 读取是 Wave 0 read-merge 通道，
   非 Wave 2 写。无 Wave 2/3/4 越界。
2. **a11y（Requirement 5）— PASS**
   `useCalendarDragSensors.ts` 同时接 `PointerSensor` + `KeyboardSensor`，测试断言
   `toHaveLength(2)`。browser smoke 实测 dnd-kit aria-live 播报
   `"Draggable item ... was dropped over droppable area 2026-05-29"`（assertive）。
3. **Click vs drag 共存 — PASS**
   `POINTER_ACTIVATION_DISTANCE_PX=5` 距离门控；chip `onClick` 保留；单测 + browser
   实测 click → Peek dialog 打开。
4. **类型安全（H4）— PASS**
   dnd-kit 类型 `import type` 引入（verbatimModuleSyntax 编译消除）；改动文件 0 命中
   `any` / `as any` / `@ts-ignore` / `@ts-expect-error` / `eslint-disable`；公共函数
   显式类型（`useCalendarDragSensors(): SensorDescriptor<SensorOptions>[]`、drag prop
   形状显式）。
5. **Fail-Fast（H7）— PASS**
   sensor / DndContext / onDragEnd 路径无 `catch`、无 silent fallback、无 `?? default`
   滥用；no-op 是显式 `setActiveDragId(null)`，不是吞掉的错误路径。
6. **Token SSOT（H11 / Req 9）— PASS**
   `--cal-drag-shadow = var(--shadow-l2)` + `--cal-drag-opacity` 落 `tokens.css` 单源；
   CSS module 仅用 `var()`，无字面 box-shadow / color-mix 硬编码 / 原型 var；
   `lint-shadow-token` / `lint-zindex-token` / `lint-hardcode` 全 clean。
7. **懒加载（08-NonFunctional §1.2）— PASS**
   `lazy(() => import('./dragDrop/MonthGridDnd'))` + 静态 `MonthGrid` 作 Suspense
   fallback（LCP 不等 dnd chunk）；`MonthEventChip` 仅 type-only dnd 导入，运行时零
   dnd 依赖，保持静态可复用。
8. **不回归 — PASS**
   `MonthEventChip` 改动纯附加（可选 `drag` prop）；Wave 0 `data-event-id` /
   `data-peek-anchor` + optimistic merge 完整；W0 的 24 测试 + W1 新 9 测试共 33 绿。
9. **提交卫生（H9）— PASS**
   4 原子提交（token +10 / impl +349-9 / css +18 / test +190），均 ≤15 文件、
   ≤400 净增，关注点分离干净，未混入 lhr 的 notion-workflow 那条线。

## 3. 独立验证结果（subagent 自跑 + Runner 复跑实证）

环境：`apps/web`，node v24.13.0，vitest 4.1.6，win32 PowerShell。

| 命令 | 结果 | 证据 |
|---|---|---|
| `npm run typecheck`（tsc -b --noEmit） | **PASS** | Exit 0 |
| `npx eslint <4 改动文件 + 2 测试>` | **PASS** | Exit 0 无输出 |
| `npx vitest run <W1 2 + W0 2 测试文件>` | **PASS** | 4 files / 33 tests passed |
| `npm run test:a11y`（vitest-axe） | **PASS** | 13/13，0 violation |
| `node scripts/lint-shadow-token.mjs` | **PASS** | 423 files clean |
| `node scripts/lint-zindex-token.mjs` | **PASS** | 423 files clean |
| `node scripts/lint-hardcode.mjs` | **PASS** | 233 files, 0 violation |
| browser smoke 1440 / 1920（Chrome MCP） | **PASS** | dragging chip / drop-over / click-peek / no-op drop + 0 console error |

## 4. 发现项

| 编号 | 严重度 | 证据 | 说明 | 建议处理 |
|---|---|---|---|---|
| F-1 | Low | `MonthGridDnd.tsx` grid/gridcell | `role=grid`/`gridcell` 未包 row/rowgroup 的 ARIA 嵌套告警，属 SIK-126/138 既有结构原样复制，非本 Wave 回归（test:a11y 13/13 仍 0 violation）。 | 非阻塞。记入 Wave 4 a11y closeout 一并修正 grid 语义。 |
| F-2 | Info | commit `799e2153c` vs `78eb92c7c` | `data-dragging` 属性（impl commit）先于其 CSS（css commit）落地，中间提交有属性无样式。 | 无需改。无 build 断裂，原子拆分的正常顺序。 |
| F-3 | Info | React 组件函数 | 组件函数无显式返回类型，依赖 JSX 推导。 | 无需改，符合仓库既有惯例。 |

无 High、无 Medium 发现项。无 scope 越界、无 silent catch、无 token SSOT 违规、
无类型逃逸、无 commit 卫生问题。

## 5. 风险等级

**Low。** 本 Wave 为拖拽骨架，drop = no-op：无写路径、无 store 形状变更、无 API/schema
触碰。`MonthEventChip` 改动纯附加（可选 prop）。可独立 revert（plan §10），回退即回到
SIK-138 read-only 月视图。dnd-kit 懒加载隔离，不进 Home 首屏 chunk。

## 6. 结论

**review pass。**

依据：scope 纪律 / a11y 双 sensor / click-drag 共存 / 类型安全 / fail-fast / token
SSOT / 懒加载 / 不回归 / 提交卫生 九项逐条 PASS；typecheck + eslint + 33 测试 + a11y
13 + 3 token linter + 1440/1920 browser smoke 自跑 PASS 实证齐全。仅 1 Low（既有 grid
ARIA 嵌套，Wave 4 修）+ 2 Info，均非阻塞。

放行 Wave 2（drop 平移计算 + 乐观 patch + `useUpdateEvent` 提交 + 回滚）。

## 7. 后续 Owner 提示

- Wave 2 起复核 W0 F-1（grid 订阅整张 optimisticEvents map，真实拖拽写入后 profiling）。
- F-1（grid/gridcell ARIA 嵌套）在 Wave 4 a11y closeout 修正。
- Wave 2 在 `onDragEnd` 接落点平移 + 乐观 + mutation 时，须保持本 Wave 的 fail-fast
  约束（detect/计算/mutation 失败抛错 + 回滚，禁 silent catch）。
