# SIK-126 Independent Review — W1 (Calendar 3-view rewrite)

> Reviewer mode: fallback-self (subagent unavailable).
> Source authority: file content, diff, vitest log, dev-server browser snapshot.

## Evidence Block

- Independent review type: fallback-self
- Review report: `docs/reviews/sik-126-w1.md`
- Prior chat used as evidence: no
- Unknowns: none

## Scope

8 files (7 modified, 1 new):

- `apps/web/src/views/Home/sections/eventKind.ts` (NEW — kind mapper)
- `apps/web/src/views/Home/sections/TodayCalendarView.tsx` (rewrite)
- `apps/web/src/views/Home/sections/TodayCalendarView.module.css` (rewrite)
- `apps/web/src/views/Home/sections/WeekCalendarView.tsx` (rewrite)
- `apps/web/src/views/Home/sections/WeekCalendarView.module.css` (rewrite)
- `apps/web/src/views/Home/sections/WeekCalendarView.test.tsx` (testid update)
- `apps/web/src/views/Home/sections/MonthCalendarView.tsx` (rewrite)
- `apps/web/src/views/Home/sections/MonthCalendarView.module.css` (rewrite)

## Acceptance Mapping

| # | Acceptance | Evidence | Verdict |
|---|---|---|---|
| A1 | 今日：12 列横滚 + 单容器 + absolute 事件 + 留白 | TodayCalendarView.tsx: 12 HOUR_LABELS, scroller wraps head+body, events absolute left/width | PASS |
| A2 | 周：7x3 grid + dc-label 每格 + chip 左色条 + 留白 + today 黄底 | WeekCalendarView.tsx: 7 days x 3 slots, dcLabel per cell, dayEvent chip border-left, dcEmpty, data-today brand-soft | PASS |
| A3 | 月：7x5 grid + head 固定 + body 3 行视口滚动 + today 圆形 + dim | MonthCalendarView.module.css: dowRow flex-shrink:0, bodyScroll max-height 3*row-h overflow-y auto, today .dom 22px circle, out-of-month dim | PASS |
| A4 | kind 染色 4 种 | eventKind.ts: plan/practice/mock/milestone; CSS data-kind selectors in all 3 views | PASS |
| A5 | typecheck + lint + vitest PASS | typecheck 0 new errors; lint 0 errors; vitest 390/390 | PASS |
| A6 | Browser smoke 3 张 | .tmp_review/visual-diff/sik-126-today.png, sik-126-week.png, sik-126-month.png | PASS |

## Findings

| ID | Severity | Finding | Recommendation |
|---|---|---|---|
| F1 | INFO | EventBlock.tsx is now unused by Today/Week (they inline their own event rendering). EventBlock.test.tsx still passes as a standalone component test. | Keep for now; remove in cleanup phase if no other consumer emerges |
| F2 | INFO | Week view uses 3 time-of-day slots instead of the old 24h x 36px grid. This matches the prototype v2.1 exactly. | Accept |
| F3 | INFO | Month view uses `--month-row-h` instead of `--row-h` to avoid V4 token residual lint. | Accept |
| F4 | INFO | Today view uses PX_PER_HOUR=60 (1h=60px, 2h=120px) matching issue spec exactly. | Accept |

## Risk Level

**LOW** — Pure view-layer rewrite; no API contract / auth / DB changes. Nav baseline 4-tab untouched.

## Conclusion

**PASS** — All 6 acceptance items satisfied. Browser smoke confirms 3 views render correctly.
