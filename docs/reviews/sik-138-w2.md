# Scope

- Mode: `Reviewer Mode`
- Review target: `SIK-138` W2 retrospective review
- Commit scope:
  - `d99ee9eec` `feat(home-calendar): add calendarViewConfig module (SIK-138 W2)`
  - `233fd8b6e` `test(home-calendar): cover calendarViewConfig factory + registry (SIK-138 W2)`
- Files reviewed:
  - `apps/web/src/views/Home/sections/calendarViewConfig/{types,errors,propertyRegistry,factory,index}.ts`
  - `apps/web/src/views/Home/sections/calendarViewConfig/{factory,propertyRegistry}.test.ts`
  - `docs/plan/sik-138-home-calendar-notion-like-v2-plan.md`
  - `docs/plan/sik-138-home-calendar-notion-like-visual-contract.md`
- Focus:
  - H6 Define-First / H11 sequencing
  - H7 fail-fast shape
  - typed error diagnostics and test coverage

# Findings

1. `High` Historical gate drift: W2 implementation merged before the visual contract landed, so the wave snapshot itself was not H11-complete.
   Evidence:
   - `types.ts@d99ee9eec:6-12` already claimed the shape was locked by `docs/plan/sik-138-home-calendar-notion-like-visual-contract.md`.
   - `docs/plan/sik-138-home-calendar-notion-like-v2-plan.md:56-61` required W1 visual contract before W2 Runner.
   - `docs/plan/sik-138-home-calendar-notion-like-visual-contract.md` first landed only at `60414c801`, after both W2 commits.
   Impact:
   - The historical W2 snapshot was not gate-compliant even though the module logic itself was reasonable.
   Resolution status on current `main`:
   - Resolved as documentation/evidence debt by the later W1/W7 closeout chain; no active runtime bug remains from this item.

2. `Medium` Historical test gap: W2 tests originally asserted error classes but did not fully lock the structured diagnostics payload for `InvalidCalendarViewError`, `InvalidCalendarLimitError`, and `UnknownCalendarPresetError`.
   Evidence:
   - `errors.ts@d99ee9eec` exposed `.value` on all typed errors.
   - Historical `factory.test.ts@233fd8b6e` only used `toThrow(ErrorClass)` for three of them.
   Impact:
   - A future regression could preserve the class while weakening the payload/message contract.
   Resolution status on current `main`:
   - Resolved in this run by strengthening `apps/web/src/views/Home/sections/calendarViewConfig/factory.test.ts` to assert `.value`, `.name`, and stable message fragments, plus a registry-order assertion for the `detail` preset.

# Visual Contract Cross-Check

| Check | Result | Evidence |
| --- | --- | --- |
| W2 config module still maps to the contract's 7 chip channels | PASS | `propertyRegistry.ts` keeps `title/kind/status/category/source/linkedSession/target` as the only accepted keys |
| W2 default/detail preset ordering still matches the contract | PASS | current `factory.test.ts` now locks `detail` against `CALENDAR_CARD_PROPERTIES` element-for-element |
| W2 currently has a real visual-contract file on disk | PASS | `docs/plan/sik-138-home-calendar-notion-like-visual-contract.md` exists; the historical sequencing gap is documented above |

# Suggested Actions

- Keep the historical sequencing drift recorded; it should not be hidden or rewritten away.
- Preserve the new typed-error assertions in `factory.test.ts`; they are the concrete remediation for the live W2 residual gap.
- No runtime code change is required for the W2 module itself.

# Risk Level

- Historical snapshot risk: `high` at merge time because H11 sequencing was broken.
- Current main residual risk: `low`.
- Reason:
  - The active repository now has the visual contract on disk.
  - The only live W2 quality gap found in this review was the typed-error assertion weakness, and it has been closed in this run.

# Decision

`review pass`

Note:
- This is a retrospective pass for current closure purposes, not a claim that the original W2 merge snapshot was flawless.
- Historical findings were either later documented (`F1`) or fixed in the current run (`F2`).
