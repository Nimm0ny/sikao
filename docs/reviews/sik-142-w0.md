---
type: review
status: pass
owner: lhr
last-reviewed: 2026-05-29
issue: SIK-142
wave: W0
reviewer: independent subagent (general-task-execution, Reviewer Mode)
target: docs/plan/sik-142-calendar-visual-unify-visual-contract.md
---

# SIK-142 W0 Review — Calendar 视觉统一视觉契约（H11 Define-First）

## 检查范围

- 审查对象：`docs/plan/sik-142-calendar-visual-unify-visual-contract.md`（W0 视觉契约，仅文档，无代码）。
- 对照基准：`docs/engineering/visual-contract-workflow.md` §2（7 节结构）+ §4（反模式红线）；`docs/vault/04-design/Prototype-Token-Map.md`（H11 强制查表）。
- 交叉核验源码：`packages/design-system/src/tokens.css`（§8 cal token + V5 语义 token）、`packages/calendar-engine/src/timezone.ts`、`apps/web/src/views/Home/sections/dragDrop/conflictGuard.ts`。
- 模式：Reviewer Mode（只读，未修改任何文件）。

## 发现项（初审）

| 编号 | 严重度 | 证据 | 说明 |
|---|---|---|---|
| F1 | **High** | 契约 §3.2 / §5 C9（初版） | 契约断言 `zonedDateKey` 在 repo「实际不存在 / 是笔误」。事实错误：`zonedDateKey` 真实存在于 `conflictGuard.ts:66`，已导出、被 `checkDropConflicts` 使用、有 `conflictGuard.test.ts` 覆盖、SIK-139 引入且 H7-compliant（解析失败抛错）。真实情况是 repo 同时有两个本地日 helper（引擎层 `toLocalDateStamp` 不抛错 + 应用层 `zonedDateKey` 抛错）。该项落在 §5「真相源」节且关联 H7 时区正确性，故定 High。 |
| F2 | Medium | §7.2 / §7.3（初版） | 截图归档路径 `.tmp_review/out/sik-142-w<N>/` 偏离 workflow §2.7 强制路径 `.tmp_review/visual-diff/<sik>/`，未记冲突。 |
| F3 | Low | §3.2 tone 表 | skipped 与 overdue 配色完全相同（同 disabled/sunken 灰），仅靠删除线区分，契约未显式说明此为有意设计。 |
| F4 | Low | §3.3 / §4.2 | chip 几何终值（radius 5 / border 3）推到 W1，存在 px 硬编码风险，未在契约约束「必须走 token」。 |
| F5 | Low | §4.1 | `--color-bg-elevated` 用途列「chip 默认底」与 §3.2 tone bg 覆盖关系未说明，易误读为逐 kind 保留。 |
| F6 | Low | §3.2 | overdue 用 occurrence 结束日、today/future 用 occurrence 起始日，两套锚点混用未定义清楚。 |

## 初审结论

**review fail** —— F1（High）未处理：契约文档事实错误，把真实存在的 helper 冲突误写成笔误，且关联 H7 时区正确性。

## 已核验 PASS 的项（初审）

- 7 节结构齐全（§1 Layout Topology / §2 Required Interactive Elements / §3 Information Density / §4 Token Map / §5 SSOT Conflicts / §6 Visual Drift / §7 Acceptance Hooks）。
- §3.2 五 tone 级联（done > skipped > overdue > today > future）完整、命中即停逻辑自洽。
- §4.2 的 15 个 `--cal-event-{tone}-{border,bg,text}` token 全部转引真实存在的 V5 语义 token（9 个被引 token 已在 tokens.css 核对：`--color-state-ok`/`-soft`、`--color-text-disabled`、`--color-bg-sunken`、`--color-brand-primary`/`-soft`、`--color-bg-surface`、`--color-text-primary`、`--color-text-meta`），无 hex / 无 color-mix 百分比。
- §6 Drift D1-D8 均带 lhr 2026-05-29 拍板日期。
- §3.2 H7 逻辑完整（本地日 + Asia/Shanghai + 禁 UTC 裸比 + 跨日 slice.day + 解析失败抛错）；timezone.ts:19 行号准确。
- §7 每 wave 有验收项 + test:a11y 命令；无 §4 红线反模式。
- 整体风险：Medium（契约结构与 token 映射质量高，唯 F1 文档准确性缺陷阻断）。

## 复审（Runner 修复后，2026-05-29）

Runner 按初审发现项修复契约，复审逐项核对：

| 编号 | 处置 | 验证 |
|---|---|---|
| F1 (High) | §5 C9 改写为「repo 双 helper 并存」真实冲突：引擎层 `toLocalDateStamp`（不抛错）+ 应用层 `zonedDateKey`（conflictGuard.ts:66，抛错，test 覆盖）。决策改为「采用抛错语义（zonedDateKey 同款），W1 定具体落地」。§3.2 同步重写本地日 helper 选型段，删除「不存在/笔误」错误陈述。 | **PASS** — 事实陈述已修正，与 conflictGuard.ts:66 / timezone.ts:19 一致；H7 抛错要求保留。 |
| F2 (Medium) | §7.2 显式记录路径冲突，新增 §5 C10：workflow §2.7 = `visual-diff/<sik>/` vs lhr 任务卡 = `out/sik-142-w<N>/`，采用任务卡路径（lhr 显式指令优先），并写明若 lhr 要求归一则 W1 起改。 | **PASS** — 冲突显式登记 + 拍板，符合 H1。 |
| F3 (Low) | §3.2 新增说明：skipped/overdue 共用灰系为有意设计，删除线为主区分编码，lhr 拍板。 | **PASS** |
| F4 (Low) | §3.3 + §6 D5 已约束 chip 几何走 `--cal-chip-*` token，§4.2 写明 radius/border 收敛到 token（5px 用 `--radius-5` 或标注例外，W1 拍板，不直写 px）。 | **PASS**（W1 实现时 lint-hardcode 把关） |
| F5 (Low) | §4.1 增「用途列读法」注：tone bg 覆盖 chip bg，`--color-bg-elevated` 仅中性兜底。 | **PASS** |
| F6 (Low) | §3.2 新增「H7 borderline 锚点定义」：today/future 用 occurrence 起始本地日（跨日 slice.day），overdue 用 occurrence 结束本地日 < 今天。 | **PASS** |

## 复审结论

**review pass** —— F1 High 已修复（契约事实准确），F2 Medium 已显式登记冲突 + 拍板，F3-F6 Low 全部处理。无遗留 High/Medium 阻断项。

整体风险：**Low**。契约 7 节结构完整、tone token 全部转引真实 V5 语义 token、SSOT Conflicts（C1-C10）与 Drift（D1-D8）均带 lhr 拍板，H7 时区抛错语义明确。可放行 W1 Runner。

## 残留待 W1 落实（非阻断，记录跟踪）

- W1 落 `--cal-event-*` token 时确认 dark theme 对比度（暗色若不足在 §8 dark 段补，browser smoke 验 WCAG AA）。
- W1 选定本地日 helper 落地方式（直接 import `zonedDateKey` / 提为 shared util），保持抛错语义。
- W1 chip 几何 5px radius 的 token 落地（新增 `--radius-5` 或契约标注例外），过 lint-hardcode。
- today 格黄底 vs today chip 黄系叠色 WCAG AA 对比度，W1 browser smoke 验。
