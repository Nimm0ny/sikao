# Scope

- Mode: `Reviewer Mode`
- Review target: `SIK-138` W4 retrospective review
- Commit scope:
  - `a8ecfdc61` `feat(home-calendar): wire CalendarPanel via CalendarViewConfig (SIK-138 W4)`
  - `6c5b2d712` `test(home-calendar): cover CalendarViewConfig wiring (SIK-138 W4)`
- Files reviewed:
  - `apps/web/src/views/Home/sections/{CalendarPanel,WeekCalendarView,MonthCalendarView}.tsx`
  - `apps/web/src/views/Home/sections/calendarViewConfig/useCalendarViewConfig.ts`
  - `apps/web/src/views/Home/sections/calendarViewConfig/useCalendarViewConfig.test.tsx`
  - relevant W4-era tests for `MonthCalendarView` / `WeekCalendarView`
- Focus:
  - whether `CalendarViewConfig` truly replaced hardcoded wiring
  - whether Sunday-first affected both layout and query window
  - whether tests covered the panel-layer seam

# Findings

1. `High` Historical wiring bug: in the W4 snapshot, `startWeekOnMonday=false` changed headers/cell topology but did not change the query window.
   Evidence:
   - `WeekCalendarView.tsx@a8ecfdc61` used `config.startWeekOnMonday` for `buildWeekDays(...)` and `dowLabels`, but `buildViewRange('week', { anchorDate, timeZone: TZ })` omitted it.
   - `MonthCalendarView.tsx@a8ecfdc61` did the same for `buildMonthCells(...)` and month DOW labels while omitting the flag from `buildViewRange('month', ...)`.
   - `packages/calendar-engine/src/view.ts` treats missing `startWeekOnMonday` as Monday-first.
   Impact:
   - The historical W4 snapshot could render a Sunday-first grid while still fetching Monday-first data.
   Resolution status on current `main`:
   - Already fixed by later calendar work; current `MonthCalendarView.tsx:198-205` and `WeekCalendarView.tsx:227-234` pass `startWeekOnMonday` into `buildViewRange(...)`.
   - This run also added explicit Sunday-first request-parameter assertions in:
     - `MonthCalendarView.test.tsx`
     - `WeekCalendarView.test.tsx`

2. `Medium` Historical seam gap: W4 tests covered hook-level/default-level behavior, but not enough production wiring around the panel/query boundary.
   Evidence:
   - Historical `useCalendarViewConfig.test.tsx` verified the hook in isolation.
   - Historical view tests passed hand-built configs directly to child views.
   - They did not assert the actual `/api/v2/plans/events` request window for Sunday-first.
   Resolution status on current `main`:
   - Partially closed in this run by adding request-parameter assertions at the month/week view level.
   - Fully closed in this run by adding a dedicated `CalendarPanel.test.tsx` write-path assertion that tab changes switch `currentView` and persist the expected payload `patchPreferences({ homeCalendarView: 'month' })`.

# Visual Contract Cross-Check

| Check | Result | Evidence |
| --- | --- | --- |
| Sunday-first visual order is honored by the active month/week views | PASS | current month/week tests assert Sunday-first headers |
| Sunday-first data window now matches Sunday-first layout | PASS | new current-main tests assert `from/to` on `/api/v2/plans/events` for both month and week |
| W4 still resolves config centrally through `CalendarPanel` / `useCalendarViewConfig` | PASS | current `CalendarPanel.tsx:63-73` and `:112-127` keep the panel as the config owner |
| W4 write-path now locks the persisted payload path | PASS | current `CalendarPanel.test.tsx` asserts tab switch persists `{ homeCalendarView: 'month' }` through `patchPreferences(...)` |

# Suggested Actions

- Keep the historical W4 bug recorded; it was real at the time and later fixed downstream.
- Preserve the new month/week query-window tests; they are the active guardrail that the original W4 lacked.
- No additional live-runtime action is required for the W4 residuals reviewed here.

# Risk Level

- Historical snapshot risk: `high`.
- Current main residual risk: `low`.
- Reason:
  - The historical Sunday-first bug was real.
  - The current repository already carries the runtime fix, and this run added both the missing request-window regression tests and the panel write-path seam test.

# Decision

`review pass`

Note:
- The pass applies to current closure of the review debt, not to the original W4 merge snapshot in isolation.
- The historical blocker was later fixed and is no longer unhandled on `main`.
