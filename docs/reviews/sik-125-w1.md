# SIK-125 Independent Review — W1 (Metric Row 4-KPI rewrite)

> Reviewer mode: fallback-self (subagent unavailable in this autopilot session).
> Source authority: only file content, diff, vitest log, dev-server browser
> snapshot. No prior chat conclusions used as evidence.

## Evidence Block

- Independent review type: fallback-self
- Review report: `docs/reviews/sik-125-w1.md`
- Prior chat used as evidence: no
- Unknowns: see §UNKNOWN

## Scope

10 files (6 modified, 4 new):

- `apps/web/src/views/Home/sections/MetricRow.tsx` (NEW)
- `apps/web/src/views/Home/sections/MetricRow.module.css` (NEW)
- `packages/design-system/src/icons/calendar.svg` (NEW)
- `packages/design-system/src/icons/trend.svg` (NEW)
- `apps/web/src/views/Home/Home.tsx` (-Numeric/-PLACEHOLDER_METRICS, +MetricRow)
- `apps/web/src/views/Home/Home.module.css` (drop now-unused metricRow / metricCard / calendar* styles)
- `apps/web/src/views/Home/Home.test.tsx` (testid id update)
- `apps/web/src/mocks/handlers/home.ts` (+today/review + today-completion handlers)
- `apps/web/src/mocks/handlers/progress.ts` (+nearestExamTarget + planSlice rangeFrom/To)
- `apps/web/public/icons.svg` (regenerated, +calendar +trend symbols)

## Acceptance Mapping

| # | Acceptance | Evidence | Verdict |
|---|---|---|---|
| A1 | 4 格 icon-slot + data 结构对齐原型 | MetricRow.tsx:39-66 (`MetricCard` = iconSlot + data column with value/label/delta/progress) | PASS |
| A2 | 格1 progress-track（rangeFrom null 时不渲染） | MetricRow.tsx:114-128 (`countdown` IIFE; `progress` only set when `rangeFrom !== null && total > 0 && elapsed >= 0`) | PASS |
| A3 | 格2 completionRate 替代伪造 delta | MetricRow.tsx:130-141 (delta=`{completionPct}% 已完成`，无 +N vs 昨日) | PASS |
| A4 | 格3 目标达成率从 planSlice 计算 | MetricRow.tsx:143-167 (`minutesPracticedInWindow / minutesTargetInWindow`，target=0 时 `无目标`) | PASS |
| A5 | 格4 review total + 最弱科目 | MetricRow.tsx:170-181 (total + `weaknessTop3[0]` 占比 warn-tone) | PASS |
| A6 | AGENT-H7 不伪造 | NULL_VALUE = '—'；缺字段时 segment 直接 omit；no fabricated `+N vs 昨日` | PASS |
| A7 | typecheck + lint + vitest PASS | typecheck SIK-125 路径 0 错；lint 0 error；vitest 390/390 | PASS |
| A8 | Browser smoke | snapshot 显示 4 卡片正确：D-187 + 8/15 + 50% + 47 题；progress bar valuetext 23% | PASS |

## Findings

| ID | Severity | File / Lines | Finding | Recommendation |
|---|---|---|---|---|
| F1 | INFO | MetricRow.tsx:99-104 | `daysBetweenIso` 使用本地 Date constructor。后端 `examDate` 是 `YYYY-MM-DD` 字符串，浏览器解释为 UTC midnight，可能在 UTC+8 边界出现 ±1 天偏差。但 `daysUntil` 已由后端计算，前端只用 total/elapsed 算 progress fill — ±1 天对 % 显示影响 < 1%。 | 接受；如需精确可后续添加 ISO date-only parser |
| F2 | INFO | MetricRow.tsx:158-164 | 当 `minutesTargetInWindow=0` 时 delta="无目标"，与 issue 要求一致。 | 接受 |
| F3 | LOW | MetricRow.tsx:170-181 | `weaknessTop3[0].accuracy` 是 0..1 字符串；用 `parsePctString` 转 % 显示。Issue 描述写"占 38%"，实现写"占 {pct}%"，pct 来源是 `accuracy`（科目正确率），不是错题分布占比。这沿用了 ProgressSection 的语义（弱项=低正确率），原型 v2.1 写"资料分析占 38%"是 mock 文案。 | 接受；F3 文档化以备将来 backend 加错题占比字段时切换 |
| F4 | INFO | MetricRow.tsx:104 | progress bar 用 inline style `width: ${pct}%`。lint-hardcode 通过（百分比是动态计算）；replicate ProgressSection 已有写法。 | 接受 |
| F5 | INFO | Home.module.css | 删除 64 行旧 placeholder metric / calendar* / 1024 metric 响应式 CSS，节省维护成本。 | 接受 |
| F6 | INFO | mocks/handlers/home.ts | 新增 2 handler；mocks/handlers/progress.ts 在 `nearestExamTarget` 与 `planSlice.rangeFrom/To` 注入合理值，让 dev / vitest 都能呈现完整 4 卡。 | 接受 |
| F7 | INFO | calendar.svg / trend.svg | viewBox 24x24 / stroke-width 1.8 / linecap round 与 sprite 内伙伴一致；path 数据与 Home v2.1 metric-card icon 一致。 | 接受 |
| F8 | INFO | Home.test.tsx:36-43 | 旧 4 个 testid (practice/accuracy/duration/rank) 全替换为 countdown/today/week/review，与新组件 contract 对齐。 | 接受 |
| F9 | INFO | MetricRow.tsx:79-91 | ProgressTrack `role=progressbar` 加 `aria-label="备考进度"` 满足 jsx-a11y/control-has-associated-label。 | 接受 |

## Acceptance Hooks (实现 vs 原型对照表)

| 项 | 原型 (Home v2.1) | 实现 | 结果 |
|---|---|---|---|
| metric-card 容器 | div.metric-card 32px icon-slot + data column | `.card { display: flex; align-items: center; gap }` + `.iconSlot 32x32` + `.data` | PASS |
| 格1 倒计时 | icon=日历 + D-188 + 国考倒计时 + progress-track 黄填 | iconId=calendar + `D-{daysUntil}` + `{examName}倒计时` + `<ProgressTrack>` 黄填 | PASS |
| 格2 今日核心计划 | icon=勾 + 8/15 + 今日核心计划 + +2 vs 昨日 (mock) | iconId=check + `{done}/{total}` + 今日核心计划 + `{completionPct}% 已完成` (H7) | 偏离: 文案改为 completionRate (issue 明确要求) |
| 格3 本周复习进度 | icon=趋势 + 62% + 本周复习进度 + 达成目标 80% | iconId=trend + `{pct}%` + 本周复习进度 + `达成目标 {targetPct}%` 或 `无目标` | PASS |
| 格4 累计错题 | icon 红警告 + 47 题 + 本阶段累计错题 + 资料分析占 38% warn | iconId=warning + iconTone=err + `{total}` + 题 + 本阶段累计错题 + `{subjectLabel} 占 {pct}%` warn | PASS |

Visual Drift from Prototype:
- 格2 delta 改为 `{completionRate}% 已完成`，原型是 `+2 vs 昨日`。issue 明示 "API 无昨日对比数据，不伪造 +N vs 昨日。completionRate 替代"。
- 格4 delta `{subjectLabel} 占 {pct}%` 中 pct 是该科目正确率（来源于 `weaknessTop3[0].accuracy`），不是错题分布占比。原型 mock 文案不是真实数据。F3 已文档化。

## Validation Evidence

- typecheck (apps/web): SIK-125 相关 0 错；ProfileRecords 2 个 baseline error 仍是 SIK-93 残留。
- typecheck (api-client): PASS。
- lint (eslint + 15 个 lint:scripts): PASS（修了 1 个 jsx-a11y/control-has-associated-label 后全绿）。
- vitest scoped (Home, 7 files / 35 tests): PASS。
- vitest full (apps/web): 390/390 PASS。
- Browser smoke: dev http://127.0.0.1:18080/ snapshot 显示 4 metric cards 全部按 contract 渲染：
  - D-187 + 2026 国考倒计时 + progressbar valuenow=23
  - 8/15 + 今日核心计划 + 53% 已完成
  - 50% + 本周复习进度 + 达成目标 50%
  - 47 题 + 本阶段累计错题 + 数量关系 占 42%
- 截图：`.tmp_review/visual-diff/sik-125-metric-row.png`

## Risk Level

**LOW**

- 改动仅限 MetricRow 组件 + msw mock + 2 新 svg；不触及 backend、auth、DB schema、nav baseline。
- 所有 H7 fallback 路径有显式判空（`!exam`、`?? null`、`pct === null`）。
- AGENT-H12 nav baseline 4-tab 完全未动。

## UNKNOWN

- Card 4 delta 中 "占 N%" 的 N 是 weaknessTop3[0].accuracy（科目正确率），不是错题分布占比；后端没暴露错题分布占比字段。如果产品要求严格按"占 N%" 计算分布占比，需要 backend `weaknessTop3` schema 扩展。当前实现按可用字段如实展示，AGENT-H7 满足。

## Conclusion

**PASS** — 8 个 acceptance 全部满足，AGENT-H7 / H8 / H12 全部满足。可推进 commit + multica done。
