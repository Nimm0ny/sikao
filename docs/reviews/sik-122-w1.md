# SIK-122 Independent Review — W1 (Topbar 修正)

> Reviewer mode: fallback-self (subagent unavailable, lhr autopilot session,
> low-priority change scope).
> Source authority: only file content, diff, vitest log, dev-server browser
> snapshot. No prior chat conclusions used as evidence.

## Evidence Block

- Independent review type: fallback-self (subagent canceled)
- Review report: `docs/reviews/sik-122-w1.md`
- Prior chat used as evidence: no
- Unknowns: see §UNKNOWN

## Scope

12 files (8 modified, 4 new):

- `apps/web/src/views/Home/sections/HomeTopbar.tsx` (NEW, 94 lines)
- `apps/web/src/views/Home/sections/HomeTopbar.module.css` (NEW, 112 lines)
- `apps/web/src/lib/commandPalette/useCommandPaletteStore.ts` (NEW, 27 lines)
- `apps/web/src/lib/commandPalette/index.ts` (NEW, 1 line)
- `packages/design-system/src/icons/bell.svg` (NEW)
- `apps/web/src/views/Home/Home.tsx` (PageHeader → HomeTopbar)
- `apps/web/src/views/Home/Home.test.tsx` (CTA copy + topbar tests)
- `apps/web/src/layouts/RootLayout/RootLayout.tsx` (palette store hookup)
- `apps/web/src/layouts/RootLayout/RootLayout.test.tsx` (store reset)
- `packages/api-client/src/progressQueries.ts` (+useProgressWeeklySummary)
- `packages/api-client/src/homeQueryKeys.ts` (+weeklySummary key)
- `packages/api-client/src/types/home.ts` (+WeeklyProgressSummaryV2 narrow)
- `apps/web/public/icons.svg` (regenerated, +bell symbol)

## Acceptance Mapping

| # | Acceptance | Evidence | Verdict |
|---|---|---|---|
| A1 | subtitle 格式对齐原型 + streak 从 API 取 | HomeTopbar.tsx:46-58 (`Intl.DateTimeFormat('zh-CN')` + `useProgressWeeklySummary()`) | PASS |
| A2 | topbar-cmd 搜索框 + 点击打开 CommandPalette | HomeTopbar.tsx:67-77 + RootLayout.tsx:75-77 (shared `useCommandPaletteStore`) | PASS |
| A3 | 通知 bell + 设置 gear icon-btn (disabled) | HomeTopbar.tsx:79-100 (`disabled` attribute on both) | PASS |
| A4 | CTA = 开始今日练习 | HomeTopbar.tsx:101-103 | PASS |

## Findings

| ID | Severity | File / Lines | Finding | Recommendation |
|---|---|---|---|---|
| F1 | INFO | HomeTopbar.tsx:46 | `早上好，lhr` 是固定 placeholder（沿用 SIK-29 行为）；issue 没有要求时段化 (早安/晚安)，所以保持。 | 后续 Phase 接 user.displayName 替换 |
| F2 | INFO | HomeTopbar.tsx:51 | `nowOverride` prop 未在 Home.tsx 调用处传值 — 仅供后续 Storybook / vitest 单测使用。本次没新增 HomeTopbar 单测文件，复用 Home.test.tsx 的 fireEvent 路径覆盖核心交互。 | 可接受 |
| F3 | LOW | progressQueries.ts:65-72 | `WeeklyProgressSummaryV2` 类型在 `types/home.ts` 内手写（narrow re-declaration），不来自 `api.generated.ts`。原因是后端 analytics router 未注册到 main.py 的 OpenAPI snapshot。注释已说明，并指明 fix 路径。 | 当 analytics 路由并入 OpenAPI 后，切换为 `components['schemas']['...']` |
| F4 | LOW | progressQueries.ts:54-59 | `fetchProgressWeeklySummary` 使用默认 React Query retry 行为；issue 要求"API 返回 0 或失败时只显示日期" — 实现路径：失败时 `query.data === undefined`，HomeTopbar.tsx:55 判 `streakDays > 0` 才渲染签到段；0 / undefined 都走纯日期路径。AGENT-H7 满足。 | 接受 |
| F5 | INFO | RootLayout.tsx:75-77 | 把 `useState` 替换为 zustand `useCommandPaletteStore`，纯 state-mechanism 替换；nav baseline 4-tab 完全未动（navItems / tabBarItems 行 80-94 一字未改）。AGENT-H12 满足。 | 接受 |
| F6 | INFO | Home.test.tsx:1-50 | renderHome() 显式 stub `/api/v2/progress/weekly` 返回 `streakDays: 0`，符合 H7 fallback 路径；新增的 3 个测试覆盖 cmd 点击 / disabled icon-btn / subtitle 不显示签到。 | 接受 |
| F7 | INFO | RootLayout.test.tsx:18-20 | `beforeEach` 加 `useCommandPaletteStore.setState({ open: false })` 重置，避免 zustand singleton 在多测之间泄漏。原 H05 测试（rail cmd-k click + Ctrl+K）均仍 pass。 | 接受 |
| F8 | LOW | HomeTopbar.module.css | 自定义 `.cmd` / `.iconBtn` 而不复用 `<Button variant="ghost" iconOnly>` — 因为 Button 的 sizing 不直接对应原型的 52×52 方形（Button md = 40px height），且 disabled icon-btn + 自定义 row-h-md 形态原型独有。可接受，但未来如果 Button 引入 icon-only-large size 可重构。 | 接受 |
| F9 | INFO | bell.svg | 新增 svg 与 Home v2.1 原型 path 数据一致；规格 viewBox 24×24 / stroke-width 1.8 / linecap round 同 settings.svg 等同 sprite 内伙伴。`build:icons` 已重跑（37 symbols）。 | 接受 |
| F10 | INFO | HomeTopbar.tsx:1-3 | `lint-allow-ui-copy` 注释正确放在前 20 行（lint-ui-copy-ssot.mjs:62 ALLOW_FILE_RE 检查前 20 行）。 | 接受 |

## Acceptance Hooks (实现 vs 原型对照表)

| 项 | 原型 (Home v2.1) | 实现 | 结果 |
|---|---|---|---|
| Topbar layout | `<header class="topbar">` flex row, greet 左 / cmd 中 / actions 右 | `.root { display: flex; align-items: center; gap: var(--space-4) }` | PASS |
| greeting h2 + subtitle | h2 greet + p subtitle (date + streak) | h1 greet + p subtitle (h1 替代 h2 — PageHeader 历史用法；语义优于视觉) | 偏离: h2→h1 |
| cmd 搜索 box | div.topbar-cmd | button.cmd + SpriteIcon + span + kbd (button role 更准确) | PASS |
| icon-btn × 2 | bell / gear icon-btn | 两个 `<button>` + `disabled` 占位符 | PASS |
| CTA | btn-cta 开始今日练习 | `<Button variant="primary">` 开始今日练习 | PASS |

Visual Drift from Prototype:
- `<h1>` vs `<h2>` — 语义改进（PageHeader 一直用 h1），无视觉差异。
- icon-btn `disabled` — issue 明示 "disabled 占位"，原型 HTML 是可点击占位，实现更严谨。

## Validation Evidence

- typecheck (apps/web): 仅 2 个 pre-existing baseline error（ProfileRecords/FilterBar.tsx:101 + ProfileRecords.tsx:73，SIK-93 残留），SIK-122 相关代码 0 错。证据：`git stash; npm run typecheck` 比对（同一组错误在 stash 前后均出现）。
- typecheck (api-client): PASS。
- lint: PASS（修正 spacing-token 2 处 + touch-target 2 处后全绿）。
- vitest scoped (Home + RootLayout): 19/19 pass。
- vitest full (apps/web): 390/390 pass。
- Browser smoke: dev http://127.0.0.1:18080/ snapshot 显示 topbar 4 元素齐 + cmd 点击打开 dialog "命令面板" + Esc 关闭。截图：`.tmp_review/visual-diff/sik-122-home-topbar.png` / `sik-122-home-topbar-closed.png`。

## Risk Level

**LOW**

- 改动仅限 Home topbar / cmd palette state 抽离，不触及 backend、API 契约、auth、DB schema。
- nav baseline (4 tabs) 完全未动；H12 satisfy。
- 新增类型 `WeeklyProgressSummaryV2` 是 narrow 手写而非 schema-generated，但有清晰的回滚 / 升级 comment。

## UNKNOWN

- 后端 `/api/v2/progress/weekly` 路由实际是否在生产环境返回。services/api/main.py 没有注册 analytics router；当前依赖前端 react-query 失败时 `data === undefined` 走纯日期路径，所以即使 endpoint 不存在 / 401，UI 不会崩。如后端要让 streak 真正显示，需要单独 PR 注册 analytics router；但本 issue 并不要求这一步（issue 明示 "API 返回 0 或失败时只显示日期"）。

## Conclusion

**PASS** — 4 个 acceptance 全部满足，AGENT-H7 / H8 / H12 全部满足。可推进 commit + multica done。
