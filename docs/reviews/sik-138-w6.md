# Scope

- Mode: `Reviewer Mode`
- Review target: `SIK-138` W6 retrospective review
- Commit scope:
  - `97da3f224` `feat(home-calendar): add CalendarPeek state core`
  - `fc48d97bb` `feat(home-calendar): add CalendarPeek sub-components`
  - `fce368a9b` `feat(home-calendar): add CalendarPeekCard shell`
  - `b3f1f0305` `feat(home-calendar): wire MonthCalendarView to CalendarPeek`
  - `690867521` `test(home-calendar): cover CalendarPeek state machine + card surface`
- Files reviewed:
  - `apps/web/src/views/Home/sections/peek/**`
  - W6-era `MonthCalendarView` peek wiring
  - W6 tests for provider/card surface
- Focus:
  - focus trap / restore focus / body scroll lock
  - read-only boundary
  - prev / next state machine
  - critical integration seams

# Findings

- No blocking findings.

1. `Low` W6 lacked a dedicated integration test for the combined `FocusTrap + provider restore-focus` seam.
   Evidence anchors:
   - `apps/web/src/views/Home/sections/peek/CalendarPeekProvider.tsx@690867521:65-75`
   - `apps/web/src/views/Home/sections/peek/CalendarPeekCard.tsx@690867521:94-119`
   - `apps/web/src/components/system/FocusTrap/FocusTrap.tsx:35-70`
   - `apps/web/src/views/Home/sections/peek/CalendarPeekCard.test.tsx@690867521:123-170`
   Evidence:
   - the provider handled deferred focus restore
   - the card relied on `FocusTrap`
   - W6 tests covered close paths and body scroll lock, but not full trap/focus-return behavior end to end

2. `Low` W6 tests did not directly cover the full `MonthCalendarView -> MonthEventChip -> peek.open(list scope)` click seam.
   Evidence anchors:
   - `apps/web/src/views/Home/sections/MonthCalendarView.tsx@690867521:112-125,151-160,184-193`
   - `apps/web/src/views/Home/sections/peek/useCalendarPeek.test.tsx@690867521:65-139`
   - `apps/web/src/views/Home/sections/peek/CalendarPeekCard.test.tsx@690867521:77-172`
   Evidence:
   - provider/card state machine tests existed
   - month-view click-to-peek integration was not yet directly asserted in the W6 review scope

# Visual Contract Cross-Check

| Check | Result | Evidence |
| --- | --- | --- |
| Read-only boundary is preserved | PASS | W6 types/components expose open/close/nav state, not inline mutation |
| Prev/next scope behavior is fail-fast / bounded | PASS | provider logic keeps list-bounded wrap/no-op semantics |
| Body scroll lock exists and has test coverage | PASS | W6 card implementation + tests covered open/close scroll lock behavior |

# Suggested Actions

- Keep the later month-view click integration tests and focus/close-path tests; they are the natural continuation of the W6 seam coverage.
- If the peek focus model is refactored again, add a dedicated test for “close returns focus to opener” so the double-restore layering stays explicit.

# Risk Level

- `Low`
- Reason:
  - W6 shipped the core read-only peek state machine correctly.
  - Residual risk sits in integration seam coverage, not in a confirmed runtime defect.

# Decision

`review pass`

`No blocking findings`
