# SIK-139 Wave 4 — Reviewer 报告

## 元信息

| 项 | 值 |
|---|---|
| Wave | W4（键盘完整改期路径 + aria-live 落点播报 + grid/gridcell ARIA 修 + F-3 收口 + 验收 closeout） |
| 审查范围 commit | `18c601fd8~1..HEAD`，HEAD = `6ddd89da2` |
| Mode | Reviewer Mode（只读审查 + 自跑验证，不改业务代码/测试） |
| 日期 | 2026-05-29 |
| Reviewer | independent subagent |

W4 commits（区间内 5 个，已逐一核对）：

- `18c601fd8` docs(spec): W4 keyboard reschedule design 段
- `741c84325` feat: keyboard reschedule engine（纯模块）
- `738f83f5b` feat: keyboard path 接线 + grid ARIA fix
- `ff060f7f4` test: grid ARIA + gate wiring(F-3) + sensor 覆盖
- `6ddd89da2` fix: anchor keyboard stepping to cell rect（browser smoke 发现的 bug 修复）

---

## 检查范围

重点文件（均在 `d:/py_pj/sikao` 下，相对仓库根）：

- `apps/web/src/views/Home/sections/dragDrop/keyboardReschedule.ts` (+ `.test.ts`)
- `apps/web/src/views/Home/sections/dragDrop/gateRescheduleDrop.ts` (+ `.test.ts`)
- `apps/web/src/views/Home/sections/dragDrop/MonthGridDnd.tsx`
- `apps/web/src/views/Home/sections/MonthCalendarView.tsx`（静态 fallback MonthGrid）
- `apps/web/src/views/Home/sections/MonthCalendarView.module.css`
- `apps/web/src/views/Home/sections/dragDrop/useCalendarDragSensors.ts` (+ `.test.tsx`)
- `apps/web/src/views/Home/sections/dragDrop/MonthGridAria.test.tsx`
- `packages/design-system/src/tokens.css`（`--cal-keyboard-ring`）
- `.kiro/specs/sik-139-calendar-drag-drop/design.md`（W4 段）

diff --stat：12 files changed, 1134 insertions(+), 106 deletions(-)。
W2/W3 核心模块（rescheduleEvent / resolveCalendarDrop / commitReschedule /
conflictGuard / runConflictGate / ConflictConfirmDialog / proposedEvent /
useRescheduleEvent）**均未出现在本区间 diff**（确认未被改写）。

---

## 逐条红线核查结论

（详见下方各节，证据引用 `文件:行号`。）

### 红线 1 — 复用而非重造（PASS）

- 键盘 Enter 确认未另造提交路径。`MonthGridDnd.tsx:317-345` 的 `handleDragEnd`
  对键盘/指针**同一条** `onDragEnd` 处理：`resolveCalendarDrop(data, overId)`
  → `gateRescheduleDrop(...)`，与 W3 完全一致；design.md W4 段亦明确标注
  「确认路径零新增：Enter 仍触发同一 onDragEnd → resolveCalendarDrop →
  runConflictGate → commitReschedule」。
- `gateRescheduleDrop.ts:60-92` 仅绑定既有 `buildProposedEvent`（`:60`）+
  `runConflictGate`（`:78`），并把三分支路由到注入的 `onCommit/onConflict/onError`。
  `onCommit` 在 `MonthGridDnd.tsx:339` 调既有 `runCommit` → `commitReschedule`
  （W2 路径，`:268-276`）。无第二条提交路径。
- import 接缝：`MonthGridDnd.tsx:48-52` 引入 `gateRescheduleDrop` +
  `buildRescheduleAnnouncements`；`useCalendarDragSensors.ts:30` 引入
  `dayCellCoordinateGetter`。仅接缝级 import，未改写 W2/W3 核心。
- git 区间 diff 确认 commitReschedule / resolveCalendarDrop / runConflictGate /
  conflictGuard / ConflictConfirmDialog / rescheduleEvent / proposedEvent /
  useRescheduleEvent **均不在本区间改动文件列表**。

结论：PASS。键盘确认严格复用 W2 commit + W3 gate，无重造。

### 红线 2 — H7 三分支隔离 + Esc/边界（PASS）

- detect 失败→不提交：`gateRescheduleDrop.ts:78-90` 经 `runConflictGate` 的
  `onError` 分支，`MonthGridDnd.tsx:341` 仅 toast，不调 `runCommit`。
  `gateRescheduleDrop.test.ts:78-87` 断言 onError 触发 1 次、onCommit/onConflict
  均未触发。
- 有冲突→开弹层不提交：`gateRescheduleDrop.ts:88` onConflict →
  `MonthGridDnd.tsx:340` `setPendingConflict`（仅持有，不写 store）；
  `gateRescheduleDrop.test.ts:68-75` 断言 onCommit 未触发。
- 无冲突→commit：`onClear`→`onCommit`→`runCommit`，`gateRescheduleDrop.test.ts:60-66`。
- Esc/取消无副作用：`handleDragCancel`（`MonthGridDnd.tsx:347-350`）仅清
  `activeDragId`/`keyboardActive`；弹层取消 `handleConflictCancel`（`:362-364`）
  仅 `setPendingConflict(null)`，无 store 写、无 PATCH。
- `stepDayCoordinate` 边界返回 undefined（不 wrap、不抛错）：
  `keyboardReschedule.ts:158-170`，非箭头键/无 collisionRect/无 anchor/无 next
  四处均 `return undefined`。`keyboardReschedule.test.ts:73-93` 覆盖左/下边界、
  非箭头键、无 collisionRect 全返回 undefined。

结论：PASS。三分支严格隔离，H7 边界语义正确。

### 红线 3 — grid ARIA 两处一致（PASS）

- MonthGridDnd（dnd 版）：`MonthGridDnd.tsx:373-417` `role="grid"` →
  `role="row"`(DOW header) + `role="rowgroup"` → 每周 `role="row"` →
  `role="gridcell"`（`DroppableCell` `:144`）。
- 静态 fallback MonthGrid：`MonthCalendarView.tsx:159-210` 同款
  grid → rowgroup → row → gridcell，`chunkIntoWeeks` 逻辑与 dnd 版同构
  （`MonthCalendarView.tsx:124-130` vs `MonthGridDnd.tsx:95-104`）。
- `display:contents` 零布局位移：`.gridRoot`（`MonthCalendarView.module.css:33-35`）
  + `.gridRow`（`:78-80`）均 `display: contents`，CSS 注释明确「zero geometry
  shift / 7 cells flow into parent CSS grid」。`.gridBody` 保持 `display:grid`
  7 列（`:62-66`）。
- 一致性由 `MonthGridAria.test.tsx:140-160` 运行时校验（grid→row→gridcell，
  42 cells，每 cell 解析到 grid 内 row 祖先）+ axe 0 violation（`:163-172`）。

结论：PASS。两处同款嵌套，Suspense 切换无 ARIA 漂移，无布局位移。

### 红线 4 — aria-live 播报（PASS）

- `buildRescheduleAnnouncements`（`keyboardReschedule.ts:213-228`）四回调：
  - `onDragStart` → `ANNOUNCE_COPY.pickup(title)`（拾起 + 方向键/回车/Esc 提示）
  - `onDragOver` → `over==null ? undefined : over(date)`（**over=null 分支返回
    undefined，不播报**，`:217-218`）
  - `onDragEnd` → `over==null ? cancelled : committed(title,date)`（`:219-223`）
  - `onDragCancel` → `cancelled`（`:224`）
- 文案单源 `ANNOUNCE_COPY`（`:189-196`），带 `lint-allow-ui-copy` marker
  （`:186-188`）。候选日期格式化 `formatCellDate`（`:204-208`），不可解析时
  回退原始 stamp（H7 不静默猜 today）。
- 接线：`MonthGridDnd.tsx:227` `useMemo(buildRescheduleAnnouncements)` →
  `:370` `<DndContext accessibility={{ announcements }}>`。
- 测试覆盖：`keyboardReschedule.test.ts:150-184` 含 over=null（onDragOver 返回
  undefined）+ 四回调文案 + 缺 title 回退「事件」。

结论：PASS。四回调文案 + over=null 分支齐备且被测。

### 红线 5 — Token SSOT（H11/R9）（PASS）

- `--cal-keyboard-ring` 单源落 `tokens.css:752-757`，值 `var(--color-focus-ring)`
  （复用 focus-ring，无新色），归在 `--cal-drag` family，注释明确「single-source
  / no new color (Requirement 9)」。
- 消费处 `MonthCalendarView.module.css:108-110` `.cell[data-keyboard-over='true']`
  `box-shadow: inset 0 0 0 2px var(--cal-keyboard-ring)`——经 token 引用，
  无原型 var 直写、无 color-mix 硬编码。
- 自跑 `lint-hardcode / lint-shadow-token / lint-zindex-token` 均 0 violation
  （见自跑验证节）。

结论：PASS。token 单源、复用 focus-ring、无硬编码。

### 红线 6 — 类型（PASS）

- 生产代码（`dragDrop/*.ts`、`MonthGridDnd.tsx`、`MonthCalendarView.tsx`）
  grep `any / as any / @ts-ignore / @ts-expect-error / as never / as unknown`：
  **无生产代码命中**（`any` 仅匹配注释词 "any cell"）。
- `as never` 仅出现在 `keyboardReschedule.test.ts:143/147/182`（构造测试用 Active/Over）；
  `as unknown` 仅出现在 `gateRescheduleDrop.test.ts:100`。均为测试文件，符合
  「生产代码不允许」。
- 公共函数显式类型：`stepDayCoordinate`（`keyboardReschedule.ts:151-154`）、
  `gateRescheduleDrop`（`gateRescheduleDrop.ts:71-76`）、`useCalendarDragSensors`
  （`useCalendarDragSensors.ts:55`）等均有显式签名与返回类型。
- typecheck 全 workspace PASS（exit 0）。

结论：PASS。无类型逃逸，类型逃逸仅限测试文件。

### 红线 7 — F-3 收口（PASS）

- W3 reviewer 要求的 gate 接线组件级集成测试由 `gateRescheduleDrop.test.ts`
  覆盖：clear→commit（`:60-66`）、conflict→hold+dialog 不 commit（`:68-75`）、
  detect 失败→onError 不 commit（`:78-87`）、proposed 携带 shifted times +
  拖拽描述字段（`:89-107`）。
- `gateRescheduleDrop.ts` 头注释明确「closes W3 review F-3」，把
  decision→gate→branch 从 `MonthGridDnd.onDragEnd` 抽到可单测 seam，
  且 H7「不引入第二条提交路径」。
- 注：jsdom 无法驱动真实 dnd drop（collision 需布局），抽 seam 单测是 W2
  抽 commitReschedule / W3 抽 runConflictGate 的同一手法，合理。

结论：PASS。F-3 已由 gateRescheduleDrop.test.ts 端到端覆盖。

---

## 发现项表

| 编号 | 严重度 | 证据 | 描述 | 建议处理 |
|---|---|---|---|---|
| F4-1 | Info | `keyboardReschedule.ts:148` | `args.context.droppableRects as ReadonlyMap<string, CellRect>`：把 dnd-kit `RectMap`（`Map<UniqueIdentifier, ClientRect>`）结构收窄为本地 `CellRect` 子集。非禁用断言（`as any/never/unknown`），CellRect 是 ClientRect 的字段子集，结构安全。 | 保留即可；若想更稳可加一行注释说明为何 narrowing 安全。无需改动。 |
| F4-2 | Info | `keyboardReschedule.ts:203` | `active.data.current as { title?: unknown } \| undefined`：把 dnd-kit drag data 收窄到带 `unknown` 属性的安全 shape，随后 `typeof === 'string'` 运行时校验。比 `any` 更安全。 | 保留。当前写法符合 fail-safe，无需改动。 |
| F4-3 | Info | `MonthGridDnd.tsx`（IDE 残留诊断） | 文件上挂着两条 axe 静态诊断（`ul[tabindex] children not allowed` / `Required ARIA parent role row not present`）。实跑 `MonthGridAria.test.tsx` axe 0 violation + `test:a11y` 13 PASS 证明这是 W4 修复前的**陈旧诊断**，已被 row 层修复，运行时无违规。 | 无需处理；属 IDE 静态扫描无法跨 DroppableCell 组件边界追踪 role 的已知局限（design.md 已说明）。 |

**Medium 及以上发现项：0。** 仅 3 条 Info，均无需改动。

---

## 自跑验证实证

cwd = `d:/py_pj/sikao/apps/web`（lint 脚本在仓库根）。

| # | 命令 | 结果 | 关键输出 |
|---|---|---|---|
| 1 | `npm run typecheck` | **PASS** | exit 0；tsc -b 跨全 workspace（@sikao/web + 6 包）无错 |
| 2 | `npx eslint src/views/Home/sections/dragDrop/ src/views/Home/sections/MonthCalendarView.tsx` | **PASS** | exit 0，无 error/warning |
| 3 | `npx vitest run src/views/Home/sections/dragDrop/` | **PASS** | `Test Files 12 passed (12)` / `Tests 73 passed (73)`（命中预期 73） |
| 4 | `npx vitest run src/views/Home/` | **PASS** | `Test Files 27 passed (27)` / `Tests 202 passed (202)`（预期 200，W4 新增 +2，无回归） |
| 5 | `npm run test:a11y` | **PASS** | `Test Files 1 passed (1)` / `Tests 13 passed (13)`，vitest-axe 0 violation |
| 6 | `node scripts/lint-hardcode.mjs` | **PASS** | exit 0，`No hardcode hard violations across 243 files` |
| 7 | `node scripts/lint-shadow-token.mjs` | **PASS** | exit 0，`shadow-token clean (444 files scanned)` |
| 8 | `node scripts/lint-zindex-token.mjs` | **PASS** | exit 0，`z-index-token clean (444 files scanned)` |
| 9 | `node scripts/lint-screen-lock.mjs` | **PASS** | exit 0，`lint-screen-lock: 0 violations` |

全部 9 条 **PASS**。

- vitest 数量说明：dragDrop 73（预期 73）；Home 202（预期 200，W4 在 dragDrop 内
  新增 keyboardReschedule(16) + gateRescheduleDrop(4) + MonthGridAria(2) + sensor(+1)
  等，相对旧基线净增，无任何旧用例转 fail）。
- 全量 vitest 下 `EssayGradingResult.test.tsx` 已知无关 flake 未在本 Wave 范围内
  触发（本 Wave 仅跑 scoped Home/dragDrop/a11y，均 PASS）。

---

## 风险等级

**低（Low）。**

- 改动集中在 Home 月视图 dnd 子模块，纯函数（keyboardReschedule / gateRescheduleDrop）
  DI 化、单测充分；接线层仅 sensors + DndContext prop + CSS hook + ARIA row 层。
- 键盘确认严格复用 W2/W3 既有提交链，无第二条 PATCH 路径，回归面小。
- token 单源、无硬编码、无类型逃逸；grid ARIA 两处同构，Suspense 切换无漂移。
- `6ddd89da2` 已修复 browser smoke 发现的 anchor 取 chip-rect 误判（改取 cell-rect），
  并补回归用例 `keyboardReschedule.test.ts:95-117`。

---

## 结论

**Review Pass。**

- 7 条红线逐条 PASS，无 Medium 及以上发现项（仅 3 条 Info，均无需改动）。
- 9 条自跑验证全 PASS（typecheck / eslint / dragDrop 73 / Home 202 / a11y 13 /
  4 个 token-lint 脚本）。
- W2/W3 核心未被改写，键盘路径复用既有 commit + gate，符合 H7 三分支隔离、
  H11/R9 token SSOT、TypeScript Strict 全部硬规则。

> 注：Review Pass 仅表示独立审查通过，不等同于 master 验收 / Notion Done；
> Evidence Block 回写与最终 closeout 由对应 owner 在主流程完成。
