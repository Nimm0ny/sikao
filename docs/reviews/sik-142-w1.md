---
type: review
status: pass
owner: lhr
last-reviewed: 2026-05-30
issue: SIK-142
wave: W1
reviewer: independent subagent (general-task-execution, Reviewer Mode)
target: SIK-142 W1 calendar chip time-status color system (6 local commits on main)
---

# SIK-142 W1 Review — Calendar chip 时间状态配色系统（H5 前端视觉 phase）

## 检查范围

- 审查对象：SIK-142 W1 的 6 个本地 commit（base `715c04f05`）：
  - `fc9918fe3` shared-utils `zonedDateKey` 抽取 + conflictGuard 引用
  - `60d5cd8` tokens.css `--radius-5` + 15 `--cal-event-*` + chip 几何收敛
  - `6b786a0` `deriveChipTone` 纯函数 + test
  - `0147498` MonthEventChip tone/kind-icon/done/通道收敛 + EventKindIcon + test + CSS
  - `0d86c06` WeekCalendarView 复用 MonthEventChip
  - `123c2d1` review F1-F3 修复（zone-correct 周 tone + 测试补充）
- 对照基准：`docs/plan/sik-142-calendar-visual-unify-visual-contract.md` §3.1/§3.2/§3.3/§4.2/§4.3/§5 C8/C9/§6 D1-D6；W0 review `docs/reviews/sik-142-w0.md`。
- 模式：Reviewer Mode（只读，未修改任何文件）。只读实跑：apps/web Home 套件 + conflictGuard + shared-utils zonedDateKey 全 PASS。

## 发现项

| 编号 | 严重度 | 证据 | 说明 | 处置 |
|---|---|---|---|---|
| F1 | **Medium** | `WeekCalendarView.tsx`（初版 `weekDaySlice` + `today` prop 用 `localStamp`/`todayStamp`） | 周视图 chip 的 tone 锚点 + today 用浏览器本地时区计算，偏离契约 §3.2「全部按 Asia/Shanghai 本地日」H7 条款，且与月视图（chip 内部 `zonedDateKey` 默认 Asia/Shanghai）时区来源不一致。真实影响受限（单机 +08:00 主机一致），但契约显式要求。 | **已修**（`123c2d1`）：`weekDaySlice` 改用 `zonedDateKey(startAt, TZ)`，`today` prop 改用 `zonedDateKey(now, TZ)`。day-header 的 `isToday` 仍是 SIK-126 既有显示逻辑，不在 W1 范围。 |
| F2 | Low | `WeekCalendarView.test.tsx`（初版仅旧 4-state 测） | 周视图复用 MonthEventChip 的只读契约无回归测试。 | **已补**（`123c2d1`）：新增「reuses MonthEventChip read-only」用例，断言渲染 `home-month-event` + 带 `data-tone` + 非 draggable。 |
| F3 | Low | `deriveChipTone.test.ts`（初版无 slice+overdue 组合） | tone 级联缺「cross-day slice 且 occurrence 已结束 → overdue」组合用例。 | **已补**（`123c2d1`）：新增该 borderline 用例。 |
| F4 | Low | `WeekCalendarView.tsx` chip 渲染 | 周视图 chip 是可聚焦但 no-op 的 `<button>`（`aria-label` 仍报「查看事件」），点击无反馈。 | **W5 缺口**（契约 §1.2/§2.2 明确 W5 接 onClick→只读 Peek），非 W1 范围，记录跟踪。 |
| F5 | Low (pre-existing) | `WeekCalendarView.tsx:119/135/140` role=grid/gridcell/row 嵌套 | axe role-nesting 违规，SIK-126 既有，非 W1 引入。 | **非 W1 范围**；W1 已实跑 `npm run test:a11y` = 13 passed / 0 violation 确认未恶化。 |

## 已核验 PASS 的项

- **H7 fail-fast**：`zonedDateKey.ts:42-47` + `deriveChipTone.ts:66/71` 坏时间戳显式抛错、无 fallback，均有专测。
- **tone 级联**：`deriveChipTone.ts:63-72` done>skipped>overdue>today>future 命中即停；三条 borderline（slice.day 锚点 / overdue 用 occurrence 结束日 / 过去开始今天结束→today）逐行符合契约 §3.2，且有测试覆盖。
- **H11 token 红线**：`MonthEventChip.module.css` / `WeekCalendarView.module.css` 无 hex / 无 color-mix % / 无原型 var（grep 仅命中注释）；15 个 `--cal-event-*`（tokens.css §8.4）全部转引真实 V5 语义 token。
- **通道收敛 §3.1**：category / status dot / source / link / target 全部从 chip 移除（`git show` 确认删除非空转），单测 `removed channels` 断言 null。
- **几何 C8**：border-left 3px + radius `var(--radius-5)`=5px + font 11px，全走 token；browser smoke 实测 computed 值一致。
- **边界**：`conflictGuard.ts` 仅改 `zonedDateKey` import + re-export 接缝，`checkDropConflicts` 编排零改动；SIK-139 `conflictGuard.test` 保持绿；无 SIK-139 拖拽核心改动。
- **H4 类型**：4 个新文件 0 处 `any` / `as any` / `@ts-ignore`；公共函数显式类型。
- **周视图只读**：chip 不传 drag / onClick，tone 锚点为 occurrence 本地日（F1 修复后 zone-correct）。

## 结论

**review pass** —— 初审条件性通过（无 High 阻断；唯一 Medium F1 = 周 tone 时区一致性）。Runner 已在 `123c2d1` 修复 F1（zone-correct）并补 F2/F3 测试缺口；F4 属 W5 范围、F5 为 pre-existing 且 test:a11y 已确认 0 violation 未恶化。复核 F1-F3 修复后无遗留 High/Medium。

整体风险：**Low**。核心交付（shared util / tone 函数 / token 单源 / chip 通道收敛 / 周视图统一）干净达标，契约 §3.1/§3.2/§3.3/§4.2/§4.3 逐项 PASS，H4/H7/H11 守住。可放行 W1 收口 + push。

## 残留待后续（非阻断，记录跟踪）

- W5：周视图 chip 接 `onClick` → 只读 Peek（F4）；source/link/target 三通道在 Peek 展示。
- W5 / 独立项：`WeekCalendarView` role=grid 嵌套 axe 结构（F5，SIK-126 既有）若要清需单独 wave。
- 月视图 day-header 的 `isToday`（SIK-126 显示逻辑）仍用浏览器本地日；非 W1 tone 范围，若未来要求全 zone-correct 可单开。
