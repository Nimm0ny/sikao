# SIK-102 W1 Review

- Date: `2026-05-27`
- Mode: `Reviewer Mode` via independent subagent
- Issue: `SIK-102`
- Scope:
  - `docs/plan/sik-102-review-frontend-data-contract.md`
  - `services/api/src/sikao_api/db/schemas_v2.py`
  - `services/api/src/sikao_api/modules/session/interface/routes.py`
  - `services/api/src/sikao_api/main.py`
  - `services/api/tests/test_postgres_practice_answers_feed_v2.py`
  - `services/api/spec/openapi.json`
  - `packages/api-client/src/types/{api.generated.ts,review.ts}`
  - `packages/api-client/src/queries/{reviewQueries.ts,causeAnalysisQueries.ts,weeklyReviewQueries.ts}`
  - `packages/domain/src/review/**`
  - `apps/web/src/mocks/handlers/{review.ts,handlers.ts}`
  - `vitest.frontend-packages.config.ts`

## Findings

- `No blocking findings`
- Follow-up re-review after OpenAPI/typegen sync and wire-shape adapter cleanup also reported `no blocking findings`

## Reviewer Notes

- `GET /api/v2/practice/answers` is now part of the checked-in OpenAPI and generated type surface, so the Review frontend no longer relies on a phantom contract from old docs.
- answer feed is now split correctly:
  - wire shape stays backend-faithful (`durationSeconds`)
  - domain adapter converts to frontend aggregation shape (`durationS`)
- package-level review tests now target the same MSW server instance the frontend harness boots, avoiding false green coverage from a disconnected interceptor.

## Residual Risk

- `Low`
- this wave mainly locks the non-visual data layer; visual fidelity to prototype and 1920 viewport proof still belongs to later `SIK-103~106` waves
- review mutation/query coverage is now broad at the domain layer, but later waves should still add view-level integration tests once ReviewToday / QuestionHub / ReviewAll stop being skeletons
- `packages/api-client/src/types/api.generated.ts` now includes additional previously-missing Notes-generated surface alongside the new Practice answer feed; before commit/push, that generated artifact still needs an H9-compliant batching decision

## Validation Evidence Reviewed

- `wsl.exe bash -lc 'cd /mnt/d/py_pj/sikao/services/api && TEST_POSTGRESQL_URL="postgresql+psycopg://postgres@127.0.0.1:5432/postgres" .venv/bin/python -m pytest -q tests/test_openapi_drift.py tests/test_postgres_practice_answers_feed_v2.py'`
- `npm run typecheck -w @sikao/api-client`
- `npm run typecheck -w @sikao/domain`
- `npm run typecheck -w @sikao/web`
- `npx eslint src/mocks/handlers.ts src/mocks/handlers/review.ts` (workdir `apps/web`)
- `npx vitest run -c vitest.frontend-packages.config.ts packages/domain/src/review/__tests__`
