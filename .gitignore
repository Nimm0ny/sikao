node_modules/
dist/
build/
.turbo/
.vite/
coverage/
*.log
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Env / Secrets
.env
.env.*
!.env.example

# IDE
.idea/
.vscode/

# IDE local state (committed by accident in earlier history; now gitignored)
docs/vault/.obsidian/workspace.json
docs/vault/.obsidian/workspace-mobile.json
.kiro/specs/**/.config.kiro

# Kiro-runtime-only steering (not part of cross-agent main rules)
.kiro/steering/independent-review.md

# Local data (本轮迁移不带数据)
/data/
*.db
*.sqlite

# Backend runtime data (assets / uploads / sqlite / imports)
services/api/var/
services/api/src/var/
database/seeds/mvp-demo/generated/

# Misc
.claude/
tmp/
.multica/
.playwright-cli/

# Playwright local artifacts (visual regression baselines + report + traces)
# Visual baseline PNGs are gitignored per apps/web/e2e/.gitignore — see that
# file for rationale. These two roots hold the run-time outputs.
apps/web/playwright-report/
apps/web/test-results/
